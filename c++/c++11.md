---
puppeteer:
  format: "a4"
  landscape: false
  scale: 1
  printBackground: true
---

<style>
@page {
  size: A4 portrait;
  margin: 2.54cm 3.18cm 2.54cm 3.18cm;
}

html,
body {
  margin: 0;
  padding: 0;
  font-family: SimSun, Songti SC, serif;
  font-size: 12pt;
  line-height: 1.5;
}

.markdown-body,
.vscode-body,
body > div {
  box-sizing: border-box;
  max-width: none !important;
  width: auto !important;
  margin: 0 !important;
  padding: 0;
}

h1,
h2,
h3,
h4,
h5,
h6 {
  page-break-after: avoid;
}

p {
  text-indent: 2em;
}

p img {
  text-indent: 0;
}

ol,
ul {
  margin-left: 2em;
  padding-left: 1.2em;
}

li {
  text-indent: 0;
}

li > p {
  text-indent: 0;
  margin: 0;
}

pre,
code {
  white-space: pre-wrap;
  word-break: break-word;
}
</style>

# C++11 / C++14 / C++17 新特性总结



## 目录

- [1 C++11](#1-c11)
  - [1.1 稳定性和兼容性](#11-稳定性和兼容性)
  - [1.2 易学和易用性](#12-易学和易用性)
  - [1.3 通用性能的提升](#13-通用性能的提升)
  - [1.4 安全性](#14-安全性)
  - [1.5 多线程](#15-多线程)
- [2 C++14](#2-c14)
- [3 C++17](#3-c17)

---

## 1 C++11

C++11 是现代 C++ 的起点。它引入了大量语言特性和标准库组件，核心方向包括：增强类型安全、减少样板代码、改善泛型编程、支持移动语义、完善资源管理、提供标准多线程库。

### 1.1 稳定性和兼容性

#### 1.1.1 原始字面量

原始字面量用于表示不需要转义的字符串，语法是 `R"(内容)"`。它适合正则表达式、Windows 路径、多行字符串、JSON/XML 片段等场景。

```cpp
#include <cstddef>
#include <iostream>
#include <string>

int main() {
    std::string path = R"(C:\Users\abc\Desktop\test.txt)";
    std::string text = R"(line1
line2
line3)";

    std::cout << path << '\n';
    std::cout << text << '\n';
}
```

如果内容中本身包含 `)"`，可以使用自定义分隔符：

```cpp
std::string s = R"tag(hello )" world)tag";
```

补充说明：

原始字面量用于表达“不想转义”的字符串，常见于正则表达式、Windows 路径、多行文本、JSON/XML 片段。普通字符串中 `\n`、`\\`、`\"` 都会被解释，原始字面量则保留文本本身。

基本形式是 `R"(内容)"`。如果内容里本来就出现 `)"`，可以使用自定义分隔符：`R"tag(内容)tag"`。

```cpp
#include <iostream>
#include <regex>
#include <string>

int main() {
    // 不需要把每个反斜杠写成 \\，Windows 路径更清楚。
    std::string path = R"(C:\Users\abc\Desktop\data.txt)";

    // 正则表达式中的反斜杠也能直接书写。
    std::regex phone(R"(\d{3}-\d{4}-\d{4})");

    // 自定义分隔符 tag 可以让字符串内部出现 )"。
    std::string text = R"tag(hello )" world)tag";

    std::cout << path << '\n' << text << '\n';
}
```

#### 1.1.2 超长整型 long long

C++11 标准化了 `long long`，它至少 64 位，适合保存较大整数。字面量后缀常用 `LL` 或 `ULL`。

```cpp
#include <iostream>
#include <limits>

int main() {
    long long a = 9223372036854775807LL;
    unsigned long long b = 18446744073709551615ULL;

    std::cout << std::numeric_limits<long long>::max() << '\n';
}
```

注意：`long long` 的范围仍然有限，处理任意大整数需要大整数库。

补充说明：

C++11 标准化了 `long long` 和 `unsigned long long`，它们至少为 64 位。字面量后缀常用 `LL`、`ULL`。在跨平台工程中，不要假设 `long` 一定是 64 位，Windows 64 位环境中 `long` 通常仍是 32 位，因此大整数更常用 `long long` 或固定宽度类型 `std::int64_t`。

```cpp
#include <cstdint>
#include <iostream>
#include <limits>

int main() {
    long long a = 9223372036854775807LL;      // 有符号 64 位常见最大值
    unsigned long long b = 18ULL * 1000 * 1000 * 1000;
    std::int64_t c = 123456789012345LL;       // 需要固定宽度时更明确

    std::cout << a << '\n';
    std::cout << b << '\n';
    std::cout << c << '\n';
    std::cout << std::numeric_limits<long long>::digits << '\n';
}
```

#### 1.1.3 类成员的快速初始化

C++11 允许在类定义中直接给非静态成员设置默认值，减少构造函数重复初始化代码。

```cpp
#include <string>
#include <utility>

class Student {
public:
    Student() = default;
    // 参数按值接收，再移动到成员中，避免不必要的字符串拷贝。
    Student(std::string name) : name_(std::move(name)) {}

private:
    // C++11 允许非静态成员直接在类内设置默认值。
    std::string name_ = "unknown";
    int age_ = 18;
    double score_ = 0.0;
};
```

如果构造函数初始化列表中也初始化了该成员，则初始化列表优先。

补充说明：

C++98 中非静态成员通常在构造函数初始化列表里初始化。C++11 允许在类内给非静态成员提供默认初始值，这叫类内成员初始化。构造函数初始化列表的值优先级更高，会覆盖类内默认值。

```cpp
#include <string>

class Config {
public:
    Config() = default;

    // 初始化列表会覆盖 port 的类内默认值。
    explicit Config(int p) : port(p) {}

private:
    int port = 8080;                 // C++11 类内成员初始化
    bool keep_alive = true;
    std::string root = "/var/www";
};
```

注意：类内初始化适合放“默认值”，构造函数初始化列表适合放“本次构造必须指定的值”。成员真正初始化顺序仍然由成员在类中的声明顺序决定，不由初始化列表顺序决定。

#### 1.1.4 final 和 override

`override` 用来明确表示派生类函数重写基类虚函数。如果签名不匹配，编译器会报错。`final` 可以禁止类继续被继承，或禁止虚函数继续被重写。

```cpp
class Base {
public:
    virtual ~Base() = default;
    virtual void run() const {}
};

class Derived final : public Base {
public:
    void run() const override {}
};
```

建议：所有重写虚函数都写 `override`，这样可以避免少写 `const`、参数类型不一致等隐蔽错误。

补充说明：

`final` 用于禁止继续重写虚函数，或者禁止类继续被继承。`override` 用于告诉编译器：这个函数必须重写基类虚函数。工程中建议所有重写虚函数都写 `override`，它能发现参数、`const`、引用限定符不一致造成的“以为重写了，实际没有重写”。

```cpp
#include <iostream>

struct Base {
    virtual void run() const {
        std::cout << "Base\n";
    }
};

struct Derived final : Base {
    // override 会检查签名是否真的匹配 Base::run。
    void run() const override final {
        std::cout << "Derived\n";
    }
};

// struct More : Derived {}; // 错误：Derived 被 final 修饰，不能再继承。
```

#### 1.1.5 模板的优化

C++11 对模板使用体验做了改进。

连续右尖括号不再需要空格：

```cpp
#include <vector>

std::vector<std::vector<int>> matrix;
```

模板别名可以用 `using` 表达，比 `typedef` 更适合模板：

```cpp
#include <map>
#include <string>

template <typename T>
using StringMap = std::map<std::string, T>;

StringMap<int> scores;
```

C++11 还支持可变参数模板，是 `tuple`、`function`、完美转发等库设施的重要基础。

```cpp
#include <iostream>

void print() {}

template <typename T, typename... Args>
void print(const T& first, const Args&... args) {
    std::cout << first << ' ';
    print(args...);
}

int main() {
    print(1, "hello", 3.14);
}
```

补充说明：

C++11 前，嵌套模板类型结尾的 `>>` 容易被解析成右移运算符，需要写成 `> >`。C++11 修复了这个问题。同时，C++11 支持函数模板默认模板参数，让模板函数的使用更灵活。

```cpp
#include <iostream>
#include <vector>

template <typename T = int>
T add(T a, T b) {
    return a + b;
}

int main() {
    // C++11 可以直接连续写两个右尖括号。
    std::vector<std::vector<int>> matrix{{1, 2}, {3, 4}};

    std::cout << add(1, 2) << '\n';          // T 推导为 int
    std::cout << add<double>(1, 2.5) << '\n';
}
```

#### 1.1.6 数值类型和字符串之间的转换

C++11 提供了 `std::to_string` 和一组字符串转数值函数。

```cpp
#include <iostream>
#include <string>

int main() {
    int n = 123;
    std::string s = std::to_string(n);

    int a = std::stoi("42");
    long b = std::stol("1000");
    long long c = std::stoll("9999999999");
    double d = std::stod("3.14159");

    std::cout << s << ' ' << a << ' ' << b << ' ' << c << ' ' << d << '\n';
}
```

`stoi` 等函数可以指定进制，也可以通过 `pos` 得到解析停止位置。

```cpp
#include <cstddef>
#include <iostream>
#include <string>

int main() {
    std::size_t pos = 0;
    int value = std::stoi("ffxx", &pos, 16);

    std::cout << value << '\n'; // 255
    std::cout << pos << '\n';   // 2
}
```

补充说明：

C++11 在 `<string>` 中提供了 `std::to_string`，也提供了 `std::stoi`、`std::stol`、`std::stoll`、`std::stoul`、`std::stof`、`std::stod`、`std::stold` 等字符串转数值函数。字符串转换失败会抛出异常：格式非法抛 `std::invalid_argument`，超出范围抛 `std::out_of_range`。

```cpp
#include <cstddef>
#include <iostream>
#include <stdexcept>
#include <string>

int main() {
    std::string s = std::to_string(12345);
    std::cout << s << '\n';

    try {
        std::size_t pos = 0;
        int value = std::stoi("0xff", &pos, 16); // 16 表示按十六进制解析
        std::cout << value << ", parsed chars = " << pos << '\n';
    } catch (const std::exception& e) {
        // 工程代码中不要忽略字符串转换异常。
        std::cout << "convert failed: " << e.what() << '\n';
    }
}
```

#### 1.1.7 静态断言 static_assert

`static_assert` 在编译期检查条件，不满足时编译失败。适合检查类型大小、模板参数要求、平台假设等。

```cpp
#include <type_traits>

template <typename T>
void onlyInteger(T value) {
    static_assert(std::is_integral<T>::value, "T must be integer");
}

int main() {
    onlyInteger(10);
    // onlyInteger(3.14); // 编译失败
}
```

运行期错误要等程序执行才发现，静态断言能把一部分问题提前到编译期。

补充说明：

普通 `assert` 是运行期检查，通常在 Debug 版本中启用。`static_assert` 是编译期检查，用于模板约束、类型尺寸检查、平台假设检查等。C++11 写法需要两个参数：条件和错误消息。

```cpp
#include <type_traits>

template <typename T>
T twice(T value) {
    // 编译期限制 T 必须是算术类型。
    static_assert(std::is_arithmetic<T>::value, "T must be arithmetic");
    return value + value;
}

int main() {
    static_assert(sizeof(void*) == 8, "this example expects 64-bit platform");
    twice(10);
}
```

#### 1.1.8 noexcept

`noexcept` 表示函数承诺不抛异常。它既能帮助编译器优化，也影响标准库容器是否优先使用移动操作。

```cpp
class Buffer {
public:
    Buffer(Buffer&& other) noexcept
        : data_(other.data_) {
        other.data_ = nullptr;
    }

private:
    char* data_ = nullptr;
};
```

也可以用 `noexcept(表达式)` 判断一个表达式是否不会抛异常。

```cpp
#include <utility>

template <typename T>
void swapValue(T& a, T& b) noexcept(noexcept(std::swap(a, b))) {
    using std::swap;
    // noexcept(表达式) 在编译期判断这个表达式是否承诺不抛异常。
    swap(a, b);
}
```

不要随意给可能抛异常的函数标记 `noexcept`，否则异常逃出时会调用 `std::terminate()`。

补充说明：

`noexcept` 表示函数承诺不抛异常。它既可以作为异常说明，也可以作为运算符查询表达式是否可能抛异常。标准容器在扩容搬移元素时会优先选择 `noexcept` 的移动构造函数，否则可能退回拷贝，以保证异常安全。

```cpp
#include <iostream>
#include <string>
#include <utility>

struct Buffer {
    std::string data;

    Buffer() = default;

    // 移动构造不抛异常，有利于 vector 等容器搬移元素。
    Buffer(Buffer&& other) noexcept : data(std::move(other.data)) {}
};

template <typename T>
void check() {
    // noexcept(...) 运算符返回 bool 常量表达式。
    std::cout << noexcept(T(std::declval<T&&>())) << '\n';
}
```

### 1.2 易学和易用性

#### 1.2.1 自动类型推导

`auto` 根据初始化表达式推导变量类型，适合迭代器、复杂模板类型、返回类型明显的场景。

```cpp
#include <map>
#include <string>

int main() {
    std::map<std::string, int> scores{{"Alice", 90}};

    // 迭代器类型很长，用 auto 可以减少冗余。
    auto it = scores.find("Alice");
    if (it != scores.end()) {
        it->second += 10;
    }
}
```

注意：`auto` 默认会丢掉顶层 `const` 和引用属性，想保留引用需要写 `auto&` 或 `const auto&`。

```cpp
const int x = 10;
auto a = x;        // int
const auto b = x;  // const int
auto& c = x;       // const int&
```

`decltype` 可以根据表达式推导类型，保留更多类型细节。

```cpp
int x = 0;
decltype(x) y = 1;      // int
decltype((x)) ref = x;  // int&
```

补充说明：

`auto` 根据初始化表达式推导变量类型。默认情况下会丢弃顶层 `const` 和引用，若要保留引用语义应写 `auto&`、`const auto&` 或 `auto&&`。`decltype(expr)` 根据表达式推导类型，尤其要注意带括号的表达式可能推导为引用。返回类型后置适合返回类型依赖参数的函数模板。

```cpp
#include <iostream>
#include <vector>

template <typename T, typename U>
auto add(T a, U b) -> decltype(a + b) {
    // 返回类型依赖 a + b 的结果类型。
    return a + b;
}

int main() {
    const int x = 10;
    auto a = x;          // a 是 int，顶层 const 被丢弃
    const auto& b = x;   // b 是 const int&

    int n = 1;
    decltype(n) c = 2;    // int
    decltype((n)) d = n;  // int&，因为 (n) 是左值表达式
    d = 3;

    std::cout << a << b << c << n << '\n';
    std::cout << add(1, 2.5) << '\n';
}
```

#### 1.2.2 基于范围的 for 循环

范围 for 简化容器和数组遍历。

```cpp
#include <iostream>
#include <vector>

int main() {
    std::vector<int> nums{1, 2, 3, 4};

    for (int x : nums) {
        // x 是元素副本，修改 x 不会影响 nums。
        std::cout << x << ' ';
    }

    for (int& x : nums) {
        // 使用引用遍历，可以直接修改容器中的元素。
        x *= 2;
    }
}
```

遍历大对象时避免拷贝：

```cpp
for (const auto& item : container) {
    // 只读，不拷贝元素
}
```

补充说明：

范围 `for` 适合遍历数组、标准容器、字符串以及自定义的可迭代对象。按值遍历会复制元素，修改不到原容器；按引用遍历才能修改元素。遍历关联容器时，元素通常是 `std::pair<const Key, Value>`。

```cpp
#include <iostream>
#include <map>
#include <string>
#include <vector>

int main() {
    std::vector<int> values{1, 2, 3};

    for (int& x : values) {
        x *= 2; // 使用引用才能修改容器中的元素。
    }

    std::map<int, std::string> users{{1, "Tom"}, {2, "Jerry"}};
    for (const auto& item : users) {
        // map 的 key 是 const，不能在遍历中修改 item.first。
        std::cout << item.first << ": " << item.second << '\n';
    }
}
```

注意：不要在范围 `for` 中随意增删当前正在遍历的容器元素，否则迭代器可能失效。

#### 1.2.3 指针空值类型 nullptr

`nullptr` 是 C++11 引入的空指针字面量，类型是 `std::nullptr_t`。它比 `NULL` 和 `0` 更安全。

```cpp
#include <iostream>

void f(int) {
    std::cout << "int\n";
}

void f(char*) {
    std::cout << "pointer\n";
}

int main() {
    f(nullptr); // pointer
    // f(NULL); // 可能调用 int 版本或产生歧义
}
```

现代 C++ 中应统一使用 `nullptr` 表示空指针。

补充说明：

`NULL` 通常是整数常量 `0` 或类似定义，容易在重载决议中被当成整数。`nullptr` 的类型是 `std::nullptr_t`，只能表示空指针，能避免 `NULL` 带来的二义性。

```cpp
#include <iostream>

void f(int) {
    std::cout << "int\n";
}

void f(char*) {
    std::cout << "char*\n";
}

int main() {
    f(0);        // 调用 f(int)
    f(nullptr);  // 调用 f(char*)
}
```

#### 1.2.4 lambda 表达式

lambda 用于定义匿名函数对象，常用于 STL 算法、回调、线程任务和局部策略。

```cpp
#include <algorithm>
#include <iostream>
#include <vector>

int main() {
    std::vector<int> nums{1, 5, 2, 8, 3};
    int threshold = 3;

    // threshold 按值捕获，lambda 内部保存的是一份副本。
    auto count = std::count_if(nums.begin(), nums.end(), [threshold](int x) {
        return x > threshold;
    });

    std::cout << count << '\n';
}
```

捕获方式：

- `[x]`：按值捕获；
- `[&x]`：按引用捕获；
- `[=]`：默认按值捕获用到的外部变量；
- `[&]`：默认按引用捕获用到的外部变量；
- `[this]`：捕获当前对象指针。

如果 lambda 需要修改按值捕获的变量副本，可以加 `mutable`。

```cpp
int n = 0;
auto f = [n]() mutable {
    // 修改的是 lambda 内部保存的 n 副本，不影响外部 n。
    ++n;
    return n;
};
```

异步或线程中谨慎使用引用捕获，避免局部变量生命周期结束后被访问。

补充说明：

lambda 是匿名函数对象。它由捕获列表、参数列表、可选的 `mutable`、异常说明、返回类型和函数体组成。捕获列表决定能访问外部哪些对象：`[=]` 值捕获，`[&]` 引用捕获，`[x, &y]` 混合捕获。

```cpp
#include <algorithm>
#include <iostream>
#include <vector>

int main() {
    int base = 10;
    std::vector<int> v{3, 1, 4, 2};

    std::sort(v.begin(), v.end(), [](int a, int b) {
        return a < b; // 无捕获 lambda 可转换为函数指针。
    });

    auto add = [base](int x) mutable {
        // mutable 允许修改值捕获的副本，不影响外部 base。
        base += x;
        return base;
    };

    std::cout << add(5) << '\n';
    std::cout << base << '\n';
}
```

### 1.3 通用性能的提升

#### 1.3.1 constexpr

`constexpr` 表示函数或变量可以在编译期求值。它适合常量计算、数组大小、模板参数等场景。

```cpp
constexpr int square(int x) {
    return x * x;
}

int arr[square(4)];
```

C++11 中 `constexpr` 函数限制较多，函数体通常只能包含一个 `return`。C++14 放宽了这些限制。

补充说明：

`const` 表示对象不可修改，但不一定是编译期常量。`constexpr` 要求表达式可在编译期求值。C++11 中 `constexpr` 函数限制较多，函数体通常只能包含一条 `return`，但它可以用于数组大小、模板实参等需要常量表达式的场景。

```cpp
#include <array>

constexpr int square(int x) {
    return x * x; // C++11 constexpr 函数体限制较严格。
}

struct Point {
    int x;
    int y;

    // constexpr 构造函数可用于构造编译期对象。
    constexpr Point(int a, int b) : x(a), y(b) {}
};

int main() {
    std::array<int, square(3)> arr{}; // 编译期计算为 9
    constexpr Point p{1, 2};
}
```

#### 1.3.2 委托构造函数和继承构造函数

委托构造函数允许一个构造函数调用同类的另一个构造函数，减少重复初始化逻辑。

```cpp
#include <string>
#include <utility>

class Person {
public:
    Person() : Person("unknown", 0) {}
    Person(std::string name) : Person(std::move(name), 0) {}
    // 所有构造逻辑最终集中到这个构造函数里。
    Person(std::string name, int age)
        : name_(std::move(name)), age_(age) {}

private:
    std::string name_;
    int age_;
};
```

继承构造函数允许派生类直接继承基类构造函数。

```cpp
class Base {
public:
    explicit Base(int x) {}
};

class Derived : public Base {
public:
    using Base::Base;
};

Derived d(10);
```

补充说明：

委托构造函数允许一个构造函数调用同类的另一个构造函数，减少初始化重复。继承构造函数通过 `using Base::Base;` 把基类构造函数引入派生类。

```cpp
#include <string>
#include <utility>

class Socket {
public:
    Socket() : Socket("127.0.0.1", 80) {} // 委托给主构造函数
    Socket(std::string h, int p) : host(std::move(h)), port(p) {}

private:
    std::string host;
    int port;
};

class TcpSocket : public Socket {
public:
    using Socket::Socket; // 继承 Socket 的构造函数
};
```

委托构造不能形成递归委托。继承构造函数只负责基类部分，派生类新增成员仍然按默认方式初始化。

#### 1.3.3 右值引用

右值引用写作 `T&&`，可以绑定到临时对象。它是移动语义和完美转发的基础。

```cpp
#include <string>

void use(const std::string& s) {
    // 接收左值和右值，但不能修改
}

void use(std::string&& s) {
    // 接收右值，可以安全“拿走”资源
}

int main() {
    std::string name = "Alice";
    use(name);              // 左值
    use(std::string("Bob")); // 右值
}
```

右值引用的核心用途不是“引用临时变量”，而是表达“这个对象资源可以被转移”。

补充说明：

右值引用写作 `T&&`，主要用于绑定临时对象、实现移动语义和完美转发。需要注意：有名字的右值引用变量本身是左值。

```cpp
#include <iostream>
#include <string>

void show(const std::string&) {
    std::cout << "lvalue or const\n";
}

void show(std::string&&) {
    std::cout << "rvalue\n";
}

int main() {
    std::string s = "hello";
    show(s);                  // s 有名字，是左值
    show(std::string("tmp"));  // 临时对象，是右值

    std::string&& rr = std::string("x");
    show(rr);                 // rr 有名字，所以是左值
}
```

#### 1.3.4 转移和完美转发

`std::move` 把对象转换成右值引用，本身不移动资源，真正移动发生在移动构造或移动赋值中。

```cpp
#include <iostream>
#include <string>
#include <utility>

int main() {
    std::string a = "hello";
    // std::move 只是把 a 转成右值，真正移动发生在 string 的移动构造中。
    std::string b = std::move(a);

    std::cout << b << '\n';
}
```

完美转发使用转发引用和 `std::forward`，能保持实参的左值/右值属性。

```cpp
#include <utility>

void process(int&) {}
void process(int&&) {}

template <typename T>
void wrapper(T&& value) {
    // std::forward 保留实参原来的左值/右值属性。
    process(std::forward<T>(value));
}

int main() {
    int x = 10;
    wrapper(x);  // 转发为左值
    wrapper(20); // 转发为右值
}
```

记忆：普通代码中想转移资源用 `std::move`；模板包装函数中想保留实参属性用 `std::forward`。

补充说明：

`std::move` 并不移动对象，它只是把表达式转换成右值引用，真正的移动发生在移动构造或移动赋值中。`std::forward<T>` 用于模板中保留实参原本的值类别，是完美转发的核心。

```cpp
#include <iostream>
#include <string>
#include <utility>

void consume(const std::string&) {
    std::cout << "copy-like\n";
}

void consume(std::string&&) {
    std::cout << "move-like\n";
}

template <typename T>
void wrapper(T&& value) {
    // T&& 在模板参数推导中是转发引用。
    consume(std::forward<T>(value));
}

int main() {
    std::string s = "hello";
    wrapper(s);              // 保留左值属性
    wrapper(std::move(s));   // 保留右值属性
}
```

#### 1.3.5 列表初始化

C++11 引入统一初始化语法 `{}`，可以用于变量、对象、容器、数组初始化。

```cpp
#include <vector>

int x{10};
std::vector<int> nums{1, 2, 3, 4};

class Point {
public:
    Point(int x, int y) : x_(x), y_(y) {}
private:
    int x_;
    int y_;
};

Point p{1, 2};
```

列表初始化可以防止窄化转换。

```cpp
int a = 3.14;  // 允许，发生截断
// int b{3.14}; // 编译错误，禁止窄化
```

补充说明：

列表初始化使用 `{}`，能统一初始化普通变量、数组、聚合类、容器和对象。它的重要特点是禁止窄化转换。若类中存在 `std::initializer_list` 构造函数，花括号初始化通常会优先匹配它。

```cpp
#include <initializer_list>
#include <iostream>
#include <vector>

class Numbers {
public:
    Numbers(std::initializer_list<int> xs) {
        for (int x : xs) {
            sum += x;
        }
    }

    int value() const {
        return sum;
    }

private:
    int sum = 0;
};

int main() {
    int a{10};
    // int b{3.14}; // 错误：列表初始化禁止窄化转换。

    std::vector<int> v{1, 2, 3};
    Numbers ns{1, 2, 3, 4};
    std::cout << a << ' ' << ns.value() << '\n';
}
```

#### 1.3.6 using 的使用

`using` 可以定义类型别名，也可以定义模板别名，比 `typedef` 更清晰。

```cpp
#include <map>
#include <string>

using String = std::string;

template <typename T>
using StringMap = std::map<std::string, T>;

StringMap<int> ages;
```

`using` 也常用于把基类重载函数引入派生类作用域。

```cpp
class Base {
public:
    void f(int) {}
    void f(double) {}
};

class Derived : public Base {
public:
    using Base::f;
    void f(const char*) {}
};
```

补充说明：

`using` 可以替代 `typedef` 定义类型别名，语法更直观；更重要的是 `using` 能定义模板别名，而 `typedef` 不能直接做到。

```cpp
#include <map>
#include <string>
#include <vector>

using String = std::string;

template <typename T>
using Vec = std::vector<T>;

template <typename T>
using StringMap = std::map<std::string, T>;

int main() {
    Vec<int> nums{1, 2, 3};
    StringMap<int> ages{{"Tom", 18}};
}
```

#### 1.3.7 可调用对象包装器和绑定器

`std::function` 是通用可调用对象包装器，可以保存函数、lambda、函数对象、成员函数绑定结果等。

```cpp
#include <functional>
#include <iostream>

int add(int a, int b) {
    return a + b;
}

int main() {
    std::function<int(int, int)> f = add;
    std::cout << f(1, 2) << '\n';
}
```

`std::bind` 可以绑定部分参数，生成新的可调用对象。

```cpp
#include <functional>
#include <iostream>

int add(int a, int b) {
    return a + b;
}

int main() {
    using namespace std::placeholders;
    // 固定第一个参数为 10，_1 表示调用 add10 时传入的第一个实参。
    auto add10 = std::bind(add, 10, _1);

    std::cout << add10(5) << '\n'; // 15
}
```

现代 C++ 中，很多 `bind` 场景可以用 lambda 替代，lambda 通常更直观。

补充说明：

可调用对象包括普通函数、函数指针、仿函数、lambda、成员函数指针等。`std::function` 是通用可调用对象包装器，常用于回调。`std::bind` 用于绑定参数、改变参数顺序或延迟调用。

```cpp
#include <functional>
#include <iostream>

int add(int a, int b) {
    return a + b;
}

struct Printer {
    void print(int x) const {
        std::cout << x << '\n';
    }
};

int main() {
    std::function<int(int, int)> fn = add;
    std::cout << fn(1, 2) << '\n';

    using namespace std::placeholders;
    auto add10 = std::bind(add, 10, _1); // 固定第一个参数为 10
    std::cout << add10(5) << '\n';

    Printer p;
    auto call_print = std::bind(&Printer::print, &p, _1);
    call_print(42);
}
```

#### 1.3.8 POD 类型

POD 是 Plain Old Data 的缩写，表示类似 C 结构体的简单数据类型。C++11 将相关概念拆分为“平凡类型”和“标准布局类型”等。POD 类型适合二进制拷贝、和 C 接口交互、共享内存结构等场景。

```cpp
#include <type_traits>

struct Data {
    int id;
    double score;
};

static_assert(std::is_pod<Data>::value, "Data should be POD in C++11");
```

注意：现代 C++ 中 `std::is_pod` 已逐渐不推荐使用，应更关注 `std::is_trivial` 和 `std::is_standard_layout`。

补充说明：

C++11 中 POD 类型可以粗略理解为同时满足“平凡类型”和“标准布局类型”的类型。平凡类型便于低层拷贝、静态初始化；标准布局类型更接近 C 语言布局，适合和 C 接口、二进制协议交互。标准库提供类型萃取判断。

```cpp
#include <iostream>
#include <type_traits>

struct Packet {
    int id;
    double value;
};

struct NonTrivial {
    NonTrivial() {} // 用户自定义构造函数会破坏平凡性。
    int id;
};

int main() {
    std::cout << std::is_trivial<Packet>::value << '\n';
    std::cout << std::is_standard_layout<Packet>::value << '\n';
    std::cout << std::is_pod<Packet>::value << '\n';
    std::cout << std::is_trivial<NonTrivial>::value << '\n';
}
```

#### 1.3.9 默认函数控制 =default 与 =delete

`=default` 明确要求编译器生成默认函数；`=delete` 明确禁止某个函数。

```cpp
class File {
public:
    File() = default;
    // 文件句柄这类资源通常不能随意复制，所以明确禁用拷贝。
    File(const File&) = delete;
    File& operator=(const File&) = delete;
};
```

常见用途：

- 禁止资源管理类被复制；
- 禁止不合理的类型转换；
- 显式恢复默认构造、拷贝、移动等行为。

```cpp
void print(int) {}
void print(double) = delete;
```

补充说明：

C++ 会为类自动生成默认构造、析构、拷贝构造、拷贝赋值、移动构造、移动赋值等特殊成员函数。`=default` 表示显式要求编译器生成默认实现，`=delete` 表示显式禁用某个函数或某个重载。

```cpp
#include <memory>

class FileHandle {
public:
    FileHandle() = default;
    ~FileHandle() = default;

    // 独占资源对象通常禁止拷贝。
    FileHandle(const FileHandle&) = delete;
    FileHandle& operator=(const FileHandle&) = delete;

    // 允许移动，把资源所有权转移出去。
    FileHandle(FileHandle&&) noexcept = default;
    FileHandle& operator=(FileHandle&&) noexcept = default;
};

void print(int) {}
void print(double) = delete; // 禁止隐式把 double 传进来。
```

#### 1.3.10 扩展的 friend 语法

C++11 对友元声明进行了扩展，允许更灵活地声明模板参数为友元。

```cpp
template <typename T>
class FriendMaker;

template <typename T>
class Box {
    friend T;
    friend class FriendMaker<T>;
};
```

友元会破坏封装边界，应只在确实需要访问私有实现时使用。

补充说明：

C++11 扩展了友元声明的灵活性，尤其是为类模板声明友元。友元不是成员函数，但可以访问类的私有成员。模板友元要明确是“所有实例都是友元”，还是“对应类型的实例才是友元”。

```cpp
#include <iostream>

template <typename T>
class Box {
public:
    explicit Box(T v) : value(v) {}

    // 当前模板参数 T 对应的 print 可以访问私有成员。
    friend void print_box(const Box<T>& box) {
        std::cout << box.value << '\n';
    }

private:
    T value;
};

int main() {
    Box<int> b(10);
    print_box(b);
}
```

#### 1.3.11 强类型枚举

`enum class` 是强类型枚举。它不会隐式转换为整数，枚举项也不会污染外层作用域。

```cpp
enum class Color {
    Red,
    Green,
    Blue
};

void paint(Color color) {}

int main() {
    paint(Color::Red);
    // paint(0); // 编译错误
}
```

也可以指定底层类型：

```cpp
enum class ErrorCode : unsigned int {
    Ok = 0,
    Failed = 1
};
```

补充说明：

传统枚举的枚举名会泄漏到外层作用域，并且可以隐式转换为整数。`enum class` 提供作用域、更强类型检查和可指定底层类型。使用时需要写 `Color::Red`，若要转成整数，需要显式转换。

```cpp
#include <cstdint>
#include <iostream>

enum class Color : std::uint8_t {
    Red = 1,
    Green = 2,
    Blue = 3
};

int main() {
    Color c = Color::Red;
    // int x = c; // 错误：enum class 不隐式转 int。
    int x = static_cast<int>(c);
    std::cout << x << '\n';
}
```

#### 1.3.12 非受限联合体

C++11 放宽了 `union` 成员类型限制，允许成员拥有非平凡构造函数的类型，但对象生命周期需要程序员手动管理。

```cpp
#include <new>
#include <string>

union Value {
    int i;
    std::string s;

    // union 中含有非平凡成员时，需要自己控制构造和析构。
    Value() {}
    ~Value() {}
};

int main() {
    Value v;
    // placement new 在 union 存储区中手动构造 string 成员。
    new (&v.s) std::string("hello");
    // 手动构造的成员也必须手动析构。
    v.s.~basic_string();
}
```

如果只是想表示“多种类型选一种”，现代 C++17 更推荐 `std::variant`。

补充说明：

C++11 放宽了 `union` 成员限制，允许联合体包含非 POD 成员。但这也意味着程序员必须更小心地管理对象生命周期：需要使用 placement new 构造当前活跃成员，并手动调用析构函数销毁它。

```cpp
#include <new>
#include <string>

union Value {
    int i;
    std::string s;

    Value() : i(0) {}   // 默认激活 i
    ~Value() {}         // 由外部决定当前活跃成员并析构
};

int main() {
    Value v;

    // 在 union 的 s 成员存储区域上显式构造 string。
    new (&v.s) std::string("hello");
    v.s.~basic_string(); // 手动析构 string 成员。

    v.i = 42;            // 重新使用 int 成员。
}
```

如果需要更安全的类型联合，C++17 应优先考虑 `std::variant`。

### 1.4 安全性

#### 1.4.1 共享智能指针 shared_ptr

`std::shared_ptr` 表示共享所有权，内部通过引用计数管理对象生命周期。

```cpp
#include <iostream>
#include <memory>

class Task {
public:
    ~Task() {
        std::cout << "destroy task\n";
    }
};

int main() {
    auto p1 = std::make_shared<Task>();
    // p2 和 p1 共享同一个 Task，引用计数增加。
    auto p2 = p1;

    std::cout << p1.use_count() << '\n';
}
```

优先使用 `std::make_shared`，它通常更高效，也能减少裸 `new` 的风险。

补充说明：

`std::shared_ptr` 用引用计数管理共享所有权。常见初始化方式包括构造函数、拷贝或移动、`std::make_shared`、`reset`。`get()` 可以取得裸指针，但不要用这个裸指针再构造另一个 `shared_ptr`，否则会造成重复释放。

```cpp
#include <iostream>
#include <memory>

struct User {
    explicit User(int i) : id(i) {}
    int id;
};

int main() {
    auto p1 = std::make_shared<User>(1); // 推荐：一次分配控制块和对象
    std::shared_ptr<User> p2 = p1;       // 引用计数增加

    std::cout << p1.use_count() << '\n';

    p2.reset(new User(2));               // p2 改为管理新对象
    User* raw = p2.get();                // 只观察，不拥有
    std::cout << raw->id << '\n';

    auto deleter = [](User* p) {
        std::cout << "delete user\n";
        delete p;
    };
    std::shared_ptr<User> p3(new User(3), deleter);
}
```

#### 1.4.2 独占智能指针 unique_ptr

`std::unique_ptr` 表示独占所有权，不能复制，只能移动。它适合表达“这个对象只有一个拥有者”。

```cpp
#include <memory>
#include <utility>

class Connection {};

std::unique_ptr<Connection> createConnection() {
    // C++11 还没有 std::make_unique，C++14 才引入。
    return std::unique_ptr<Connection>(new Connection);
}

int main() {
    auto conn = createConnection();
    // unique_ptr 不能复制，只能通过 move 转移所有权。
    auto other = std::move(conn);
}
```

作为函数参数时：

- `T&` 或 `T*`：只使用对象，不接管所有权；
- `std::unique_ptr<T>`：接管所有权；
- `const std::unique_ptr<T>&` 通常不如直接传 `T*` 或 `T&` 清晰。

补充说明：

`std::unique_ptr` 表示独占所有权，不能拷贝，只能移动。C++11 中没有 `std::make_unique`，通常直接用构造函数接管 `new` 返回的指针；C++14 起才推荐 `std::make_unique`。

```cpp
#include <iostream>
#include <memory>
#include <utility>

struct File {
    void close() {
        std::cout << "close\n";
    }
};

int main() {
    std::unique_ptr<int> p(new int(42));
    std::unique_ptr<int> q = std::move(p); // 所有权转移，p 变空

    if (!p) {
        std::cout << *q << '\n';
    }

    auto deleter = [](File* f) {
        if (f) {
            f->close();
        }
        delete f;
    };

    std::unique_ptr<File, decltype(deleter)> file(new File, deleter);
}
```

#### 1.4.3 弱引用智能指针 weak_ptr

`std::weak_ptr` 不增加引用计数，用来观察 `shared_ptr` 管理的对象。它常用于解决循环引用。

```cpp
#include <memory>

struct Parent;

struct Child {
    std::weak_ptr<Parent> parent;
};

struct Parent {
    std::shared_ptr<Child> child;
};

int main() {
    auto p = std::make_shared<Parent>();
    auto c = std::make_shared<Child>();
    p->child = c;
    // child 只弱引用 parent，不增加 parent 的引用计数，避免循环引用。
    c->parent = p;
}
```

使用 `weak_ptr` 访问对象前，需要调用 `lock()` 得到 `shared_ptr`。

```cpp
if (auto sp = c->parent.lock()) {
    // lock 成功说明 parent 对象仍然存在。
    // 对象仍然存在
}
```

补充说明：

`std::weak_ptr` 不拥有对象，不增加引用计数，主要用于打破 `shared_ptr` 循环引用，以及观察对象是否仍存在。使用对象前应通过 `lock()` 得到临时 `shared_ptr`。

```cpp
#include <iostream>
#include <memory>

struct Node {
    std::shared_ptr<Node> next;
    std::weak_ptr<Node> prev; // 弱引用避免双向链表循环引用。
};

int main() {
    auto a = std::make_shared<Node>();
    auto b = std::make_shared<Node>();
    a->next = b;
    b->prev = a;

    if (auto p = b->prev.lock()) {
        // lock 成功说明对象仍然存在。
        std::cout << "prev exists\n";
    }
}
```

如果对象需要在成员函数中返回管理自己的 `shared_ptr`，应继承 `std::enable_shared_from_this<T>`，不要直接用 `this` 构造 `shared_ptr`。

### 1.5 多线程

#### 1.5.1 chrono 日期和时间库

`std::chrono` 提供类型安全的时间表示，包括时间段、时间点和时钟。

```cpp
#include <chrono>
#include <iostream>
#include <thread>

int main() {
    using namespace std::chrono;

    auto begin = steady_clock::now();
    std::this_thread::sleep_for(milliseconds(100));
    auto end = steady_clock::now();

    std::cout << duration_cast<milliseconds>(end - begin).count() << "ms\n";
}
```

常见时钟：

- `system_clock`：系统时间，可能被调整；
- `steady_clock`：单调时钟，适合测量耗时；
- `high_resolution_clock`：高精度时钟，具体实现依赖平台。

补充说明：

`<chrono>` 包含三个核心概念：时间间隔 `duration`、时间点 `time_point`、时钟 `clock`。常见时钟有 `system_clock`、`steady_clock`、`high_resolution_clock`。测量耗时优先使用 `steady_clock`，因为它单调递增，不受系统时间调整影响。

```cpp
#include <chrono>
#include <iostream>
#include <thread>

int main() {
    using namespace std::chrono;

    auto start = steady_clock::now();
    std::this_thread::sleep_for(milliseconds(20));
    auto end = steady_clock::now();

    auto cost = duration_cast<milliseconds>(end - start);
    std::cout << cost.count() << " ms\n";
}
```

#### 1.5.2 线程类 thread

`std::thread` 用于创建线程。

```cpp
#include <iostream>
#include <thread>

void work(int id) {
    std::cout << "thread " << id << '\n';
}

int main() {
    std::thread t(work, 1);
    t.join();
}
```

线程对象析构前必须 `join()` 或 `detach()`，否则程序会调用 `std::terminate()`。

补充说明：

`std::thread` 表示一个执行线程，构造时绑定线程函数和参数。线程对象销毁前必须 `join()` 或 `detach()`，否则会调用 `std::terminate()`。传引用参数时要使用 `std::ref`。

```cpp
#include <iostream>
#include <thread>

void work(int& value) {
    value += 1;
}

int main() {
    int n = 0;
    std::thread t(work, std::ref(n)); // std::ref 表示按引用传参

    if (t.joinable()) {
        t.join(); // 等待线程结束
    }

    std::cout << n << '\n';
    std::cout << std::thread::hardware_concurrency() << '\n';
}
```

#### 1.5.3 this_thread 命名空间

`std::this_thread` 提供当前线程相关操作。

```cpp
#include <chrono>
#include <iostream>
#include <thread>

int main() {
    std::cout << std::this_thread::get_id() << '\n';

    std::this_thread::sleep_for(std::chrono::seconds(1));
    std::this_thread::yield();
}
```

常用函数：

- `get_id()`：获取当前线程 id；
- `sleep_for()`：休眠一段时间；
- `sleep_until()`：休眠到某个时间点；
- `yield()`：主动让出 CPU。

补充说明：

`std::this_thread` 提供当前线程相关操作：`get_id()` 获取当前线程 ID，`sleep_for()` 休眠指定时长，`sleep_until()` 休眠到指定时间点，`yield()` 主动让出 CPU 时间片。

```cpp
#include <chrono>
#include <iostream>
#include <thread>

int main() {
    std::cout << std::this_thread::get_id() << '\n';

    // 当前线程休眠 100 毫秒。
    std::this_thread::sleep_for(std::chrono::milliseconds(100));

    // 提示调度器可以切换到其他线程执行。
    std::this_thread::yield();
}
```

#### 1.5.4 call_once

`std::call_once` 配合 `std::once_flag` 保证某段初始化代码只执行一次，常用于线程安全单例或全局资源初始化。

```cpp
#include <iostream>
#include <mutex>
#include <thread>

std::once_flag flag;

void init() {
    std::cout << "init once\n";
}

void worker() {
    std::call_once(flag, init);
}

int main() {
    std::thread t1(worker);
    std::thread t2(worker);
    t1.join();
    t2.join();
}
```

补充说明：

`std::call_once` 配合 `std::once_flag` 保证某段初始化代码在多线程环境中只执行一次。若执行函数抛异常，本次调用不算成功，后续线程仍会再次尝试执行。

```cpp
#include <iostream>
#include <mutex>
#include <thread>

std::once_flag flag;

void init() {
    std::cout << "init once\n";
}

void worker() {
    std::call_once(flag, init);
}

int main() {
    std::thread a(worker);
    std::thread b(worker);
    a.join();
    b.join();
}
```

#### 1.5.5 互斥锁 mutex

互斥锁用于保护共享数据。

```cpp
#include <mutex>

std::mutex mtx;
int counter = 0;

void add() {
    std::lock_guard<std::mutex> lock(mtx);
    ++counter;
}
```

常用锁类型：

- `std::mutex`：普通互斥锁；
- `std::recursive_mutex`：递归互斥锁；
- `std::timed_mutex`：支持超时加锁；
- `std::lock_guard`：简单 RAII 加锁；
- `std::unique_lock`：更灵活，可配合条件变量。

补充说明：

`std::mutex` 提供 `lock()`、`try_lock()`、`unlock()`。实际工程中应优先使用 RAII 封装：`std::lock_guard` 简单可靠，`std::unique_lock` 更灵活，支持延迟加锁、解锁、配合条件变量。C++11 还提供 `std::recursive_mutex` 和 `std::timed_mutex`。

```cpp
#include <iostream>
#include <mutex>
#include <thread>
#include <vector>

int main() {
    int counter = 0;
    std::mutex mtx;

    auto task = [&] {
        for (int i = 0; i < 1000; ++i) {
            std::lock_guard<std::mutex> lock(mtx);
            ++counter; // 受互斥锁保护的临界区。
        }
    };

    std::vector<std::thread> threads;
    for (int i = 0; i < 4; ++i) {
        threads.emplace_back(task);
    }
    for (auto& t : threads) {
        t.join();
    }

    std::cout << counter << '\n';
}
```

#### 1.5.6 条件变量

条件变量用于线程等待某个条件成立，常见于生产者消费者模型。

```cpp
#include <condition_variable>
#include <mutex>
#include <queue>

std::mutex mtx;
std::condition_variable cv;
std::queue<int> q;

void producer() {
    {
        std::lock_guard<std::mutex> lock(mtx);
        // 修改共享队列必须在锁保护下进行。
        q.push(1);
    }
    // 放入数据后通知等待的消费者。
    cv.notify_one();
}

void consumer() {
    std::unique_lock<std::mutex> lock(mtx);
    // 谓词版本 wait 会在被唤醒后重新检查条件。
    cv.wait(lock, [] {
        return !q.empty();
    });

    int value = q.front();
    q.pop();
}
```

推荐使用 `wait(lock, predicate)`，它能正确处理虚假唤醒。

补充说明：

`std::condition_variable` 用于线程等待某个条件成立。它必须配合 `std::unique_lock<std::mutex>` 使用。等待时应使用带谓词的重载，避免虚假唤醒导致逻辑错误。`condition_variable_any` 可以配合其他锁类型。

```cpp
#include <condition_variable>
#include <iostream>
#include <mutex>
#include <queue>
#include <thread>

int main() {
    std::mutex mtx;
    std::condition_variable cv;
    std::queue<int> q;
    bool done = false;

    std::thread producer([&] {
        {
            std::lock_guard<std::mutex> lock(mtx);
            q.push(42);
            done = true;
        }
        cv.notify_one(); // 修改共享状态后通知消费者。
    });

    std::thread consumer([&] {
        std::unique_lock<std::mutex> lock(mtx);
        cv.wait(lock, [&] { return done && !q.empty(); });
        std::cout << q.front() << '\n';
        q.pop();
    });

    producer.join();
    consumer.join();
}
```

#### 1.5.7 原子变量 atomic

`std::atomic` 用于无数据竞争地访问共享变量。

```cpp
#include <atomic>

std::atomic<int> counter{0};

void add() {
    counter.fetch_add(1, std::memory_order_relaxed);
}
```

原子变量适合简单计数器、状态标志等。复杂共享状态优先使用互斥锁。

补充说明：

`std::atomic<T>` 用于无锁或低锁的共享变量操作，常见成员包括 `load`、`store`、`exchange`、`compare_exchange_weak`、`compare_exchange_strong`、`fetch_add` 等。简单计数可以使用原子变量代替互斥锁，但多个变量之间的不变式通常仍需要锁。

```cpp
#include <atomic>
#include <iostream>
#include <thread>
#include <vector>

int main() {
    std::atomic<int> counter{0};

    auto task = [&] {
        for (int i = 0; i < 1000; ++i) {
            counter.fetch_add(1, std::memory_order_relaxed);
        }
    };

    std::vector<std::thread> threads;
    for (int i = 0; i < 4; ++i) {
        threads.emplace_back(task);
    }
    for (auto& t : threads) {
        t.join();
    }

    std::cout << counter.load() << '\n';
}
```

默认内存序是 `std::memory_order_seq_cst`，最容易理解但可能不是性能最优。除非明确知道内存模型含义，否则不要随意使用更弱的内存序。

#### 1.5.8 线程异步

`std::async` 启动异步任务，返回 `std::future` 表示未来结果。

```cpp
#include <future>
#include <iostream>

int calc() {
    return 42;
}

int main() {
    auto f = std::async(std::launch::async, calc);
    std::cout << f.get() << '\n';
}
```

`std::future::get()` 会等待结果，并且只能调用一次。

也可以用 `std::promise` 手动设置结果。

```cpp
#include <future>
#include <thread>

int main() {
    std::promise<int> p;
    std::future<int> f = p.get_future();

    std::thread t([&p] {
        p.set_value(100);
    });

    int value = f.get();
    t.join();
}
```

补充说明：

`std::future` 表示未来会得到的结果。`std::promise` 用于在一个线程中设置结果，另一个线程通过 `future` 读取。`std::packaged_task` 把可调用对象包装成异步任务。`std::async` 可以直接异步执行函数并返回 `future`。

```cpp
#include <future>
#include <iostream>

int slow_add(int a, int b) {
    return a + b;
}

int main() {
    // launch::async 表示立即创建异步任务执行。
    auto fut = std::async(std::launch::async, slow_add, 1, 2);
    std::cout << fut.get() << '\n'; // get 会等待结果，只能调用一次。

    std::promise<int> p;
    std::future<int> f = p.get_future();
    p.set_value(10);
    std::cout << f.get() << '\n';
}
```

`std::launch::deferred` 表示延迟到 `get()` 或 `wait()` 时再执行。若不指定策略，标准库可以自行选择。

#### 1.5.9 异步线程池

线程池复用固定数量的工作线程，避免频繁创建和销毁线程。核心结构是任务队列、互斥锁、条件变量和停止标志。

```cpp
#include <cstddef>
#include <condition_variable>
#include <functional>
#include <mutex>
#include <queue>
#include <thread>
#include <utility>
#include <vector>

class ThreadPool {
public:
    explicit ThreadPool(std::size_t n) {
        for (std::size_t i = 0; i < n; ++i) {
            // 每个工作线程不断从任务队列中取任务执行。
            workers_.emplace_back([this] {
                while (true) {
                    std::function<void()> task;
                    {
                        std::unique_lock<std::mutex> lock(mtx_);
                        // 使用谓词版本 wait，避免虚假唤醒导致空队列取任务。
                        cv_.wait(lock, [this] {
                            return stop_ || !tasks_.empty();
                        });
                        if (stop_ && tasks_.empty()) {
                            return;
                        }
                        task = std::move(tasks_.front());
                        tasks_.pop();
                    }
                    task();
                }
            });
        }
    }

    ~ThreadPool() {
        {
            std::lock_guard<std::mutex> lock(mtx_);
            stop_ = true;
        }
        cv_.notify_all();
        for (auto& worker : workers_) {
            worker.join();
        }
    }

    void submit(std::function<void()> task) {
        {
            std::lock_guard<std::mutex> lock(mtx_);
            // 任务进入队列后通知一个工作线程。
            tasks_.push(std::move(task));
        }
        cv_.notify_one();
    }

private:
    std::mutex mtx_;
    std::condition_variable cv_;
    std::queue<std::function<void()>> tasks_;
    std::vector<std::thread> workers_;
    bool stop_ = false;
};
```


补充说明：

线程池的目标是复用固定数量的工作线程，避免频繁创建和销毁线程。典型设计包括任务队列、工作线程数组、互斥锁、条件变量、停止标志。提交任务时把任务包装成 `packaged_task`，返回 `future` 给调用者。

```cpp
#include <cstddef>
#include <condition_variable>
#include <functional>
#include <future>
#include <memory>
#include <mutex>
#include <queue>
#include <thread>
#include <utility>
#include <vector>

class ThreadPool {
public:
    explicit ThreadPool(std::size_t n) {
        for (std::size_t i = 0; i < n; ++i) {
            workers.emplace_back([this] {
                for (;;) {
                    std::function<void()> task;
                    {
                        std::unique_lock<std::mutex> lock(mtx);
                        cv.wait(lock, [this] { return stop || !tasks.empty(); });
                        if (stop && tasks.empty()) {
                            return;
                        }
                        task = std::move(tasks.front());
                        tasks.pop();
                    }
                    task(); // 在锁外执行任务，避免阻塞其他线程取任务。
                }
            });
        }
    }

    template <typename F>
    auto submit(F f) -> std::future<decltype(f())> {
        using R = decltype(f());
        auto task = std::make_shared<std::packaged_task<R()>>(std::move(f));
        std::future<R> ret = task->get_future();
        {
            std::lock_guard<std::mutex> lock(mtx);
            tasks.emplace([task] { (*task)(); });
        }
        cv.notify_one();
        return ret;
    }

    ~ThreadPool() {
        {
            std::lock_guard<std::mutex> lock(mtx);
            stop = true;
        }
        cv.notify_all();
        for (auto& t : workers) {
            t.join();
        }
    }

private:
    std::mutex mtx;
    std::condition_variable cv;
    bool stop = false;
    std::queue<std::function<void()>> tasks;
    std::vector<std::thread> workers;
};
```

真实线程池还会处理任务返回值、异常、队列上限、优雅关闭和任务取消。

---

## 2 C++14

C++14 的主要目标是完善 C++11，修复缺陷并提供一些提高开发效率的小特性。

### 2.1 二进制字面量

C++14 支持 `0b` 或 `0B` 开头的二进制整数字面量。

```cpp
#include <iostream>

int main() {
    int mask = 0b1010;
    std::cout << mask << '\n'; // 10
}
```

可以配合数字分隔符 `'` 提高可读性。

```cpp
int value = 0b1010'1100;
int million = 1'000'000;
```

补充说明：

C++14 增加二进制字面量，前缀为 `0b` 或 `0B`，适合位掩码、协议标志位、颜色通道等场景。C++14 还允许用单引号作为数字分隔符，提高长数字可读性。分隔符不影响数值。

```cpp
#include <iostream>

int main() {
    unsigned flags = 0b0001'0010u;
    unsigned read = 0b0000'0010u;

    if (flags & read) {
        std::cout << "read enabled\n";
    }

    int million = 1'000'000;
    std::cout << million << '\n';
}
```

### 2.2 自定义字面量

C++11 已支持用户自定义字面量，C++14 标准库增加了更多字面量支持，例如时间、字符串等。

```cpp
#include <chrono>
#include <iostream>

int main() {
    using namespace std::chrono_literals;

    auto t = 100ms;
    std::cout << t.count() << '\n';
}
```

字符串字面量：

```cpp
#include <string>

using namespace std::string_literals;

auto s = "hello"s; // std::string
```

补充说明：

自定义字面量允许为字面量添加自定义后缀，定义形式是 `operator "" _suffix`。后缀建议以下划线开头，避免与标准库保留后缀冲突。C++14 标准库还提供了一些内置字面量，如字符串字面量和 chrono 时间字面量。

```cpp
#include <chrono>
#include <iostream>
#include <string>

constexpr long long operator "" _KB(unsigned long long n) {
    return static_cast<long long>(n * 1024);
}

int main() {
    auto size = 4_KB; // 自定义字面量，表示 4096。
    std::cout << size << '\n';

    using namespace std::chrono_literals;
    auto timeout = 200ms; // 标准库 chrono 字面量。
}
```

原始字面量形式也能用于自定义后缀，适合保留字面量文本本身。

### 2.3 constexpr 限制放宽

C++14 放宽了 `constexpr` 函数限制，允许局部变量、循环、分支等。

```cpp
constexpr int sum(int n) {
    int result = 0;
    for (int i = 1; i <= n; ++i) {
        result += i;
    }
    return result;
}

static_assert(sum(100) == 5050, "wrong");
```

这让编译期计算更容易写成普通函数风格。

补充说明：

C++14 放宽了 `constexpr` 函数限制。函数体中可以声明局部变量、使用循环、条件分支、修改局部变量；`constexpr` 成员函数不再隐式为 `const`；也允许 `constexpr void` 函数。构造函数中也可以写更自然的逻辑。

```cpp
#include <array>

constexpr int factorial(int n) {
    int result = 1;
    for (int i = 2; i <= n; ++i) {
        result *= i; // C++14 constexpr 中允许修改局部变量。
    }
    return result;
}

struct Counter {
    int value = 0;

    constexpr void inc() {
        ++value; // C++14 中 constexpr 成员函数不再隐式 const。
    }
};

int main() {
    std::array<int, factorial(5)> arr{};
}
```

### 2.4 泛型 Lambda 以及捕获变量增强

C++14 允许 lambda 参数使用 `auto`，形成泛型 lambda。

```cpp
#include <iostream>

int main() {
    auto add = [](auto a, auto b) {
        return a + b;
    };

    std::cout << add(1, 2) << '\n';
    std::cout << add(1.5, 2.5) << '\n';
}
```

C++14 还支持初始化捕获，可以把表达式结果保存到捕获变量中，也常用于移动捕获。

```cpp
#include <memory>
#include <utility>

int main() {
    auto ptr = std::make_unique<int>(42);

    // 初始化捕获可以把 unique_ptr 移入 lambda。
    auto f = [p = std::move(ptr)] {
        return *p;
    };
}
```

补充说明：

C++14 允许 lambda 参数使用 `auto`，这相当于匿名函数对象的模板调用运算符。C++14 还支持初始化捕获，可以在捕获列表中创建新成员，常用于移动捕获 `unique_ptr`。

```cpp
#include <algorithm>
#include <iostream>
#include <memory>
#include <utility>
#include <vector>

int main() {
    auto less = [](const auto& a, const auto& b) {
        return a < b; // 泛型 lambda，可比较多种类型。
    };

    std::vector<int> v{3, 1, 2};
    std::sort(v.begin(), v.end(), less);

    auto ptr = std::make_unique<int>(42);
    auto task = [p = std::move(ptr)] {
        // p 是 lambda 内部成员，获得了 unique_ptr 所有权。
        std::cout << *p << '\n';
    };
    task();
}
```

### 2.5 auto 和 decltype 的改进

C++14 允许普通函数返回类型使用 `auto` 由编译器推导。

```cpp
auto add(int a, int b) {
    return a + b;
}
```

`decltype(auto)` 可以在返回时保留引用和值类别。

```cpp
int value = 10;

decltype(auto) getRef() {
    return (value); // int&
}

auto getValue() {
    return value; // int
}
```

如果函数返回引用，`decltype(auto)` 要谨慎使用，避免返回局部变量引用。

补充说明：

C++14 允许普通函数返回类型使用 `auto` 自动推导，也引入 `decltype(auto)` 来保留表达式的引用和 `const` 属性。需要特别注意 `decltype(auto)` 对括号敏感。

```cpp
#include <vector>

auto make_value() {
    return 42; // 返回类型推导为 int。
}

template <typename Container>
decltype(auto) first(Container& c) {
    return c[0]; // 保留 c[0] 的引用属性。
}

int main() {
    std::vector<int> v{1, 2, 3};
    first(v) = 10; // first 返回 int&，可以修改原容器。
}
```

`auto` 返回类型要求所有 `return` 语句推导出的类型一致。

### 2.6 聚合类成员初始化

C++14 放宽了聚合类初始化限制，允许聚合类拥有默认成员初始化器。

```cpp
struct Config {
    int port = 8080;
    bool debug = false;
};

int main() {
    Config c1;
    Config c2{9000, true};
}
```

这让简单配置结构体更容易设置默认值。

补充说明：

C++14 允许聚合类拥有类内成员初始化器后仍保持聚合特性。这样既能提供默认值，又能继续使用花括号聚合初始化。

```cpp
#include <iostream>
#include <string>

struct Server {
    std::string host = "127.0.0.1";
    int port = 80;
    bool ssl = false;
};

int main() {
    Server a;                    // 使用默认成员初始值
    Server b{"0.0.0.0", 8080};   // 未指定的 ssl 使用默认值 false

    std::cout << a.host << ':' << b.port << '\n';
}
```

### 2.7 变量模板

变量模板允许定义一组变量，典型场景是数学常量、类型 traits 简写。

```cpp
template <typename T>
constexpr T pi = T(3.1415926535897932385);

int main() {
    double a = pi<double>;
    float b = pi<float>;
}
```

标准库在 C++17 中大量提供 `_v` 形式的变量模板，例如 `std::is_integral_v<T>`。

补充说明：

变量模板允许定义依赖模板参数的变量或常量，典型用途是数学常量、类型相关常量、类型萃取的简写。它可以有默认模板参数，也可以全特化或偏特化。

```cpp
#include <iostream>
#include <type_traits>

template <typename T>
constexpr T pi = T(3.1415926535897932385);

template <typename T>
constexpr bool is_number_v = std::is_arithmetic<T>::value;

int main() {
    std::cout << pi<float> << '\n';
    std::cout << is_number_v<int> << '\n';
}
```

C++17 标准库中的很多 `_v` 形式，如 `std::is_same_v<T, U>`，就是变量模板风格。

### 2.8 透明操作符函数对象

C++14 为标准比较函数对象提供透明比较能力，例如 `std::less<>`。它可以避免容器查找时构造临时对象，也能支持异质比较。

```cpp
#include <functional>
#include <iostream>
#include <set>
#include <string>

int main() {
    std::set<std::string, std::less<>> names{"Alice", "Bob"};

    // std::less<> 是透明比较器，允许用 const char* 查找 string key。
    auto it = names.find("Alice"); // 可用 const char* 查找
    if (it != names.end()) {
        std::cout << *it << '\n';
    }
}
```

`std::less<>` 中省略模板参数后，会根据实参类型自动调用合适的 `<` 比较。

补充说明：

C++14 为标准函数对象提供透明版本，如 `std::less<>`、`std::greater<>`、`std::equal_to<>`。透明比较器不固定参数类型，可以减少临时对象构造，并支持有序关联容器的异构查找。

```cpp
#include <iostream>
#include <set>
#include <string>

int main() {
    std::set<std::string, std::less<>> names{"alice", "bob"};

    // 使用透明比较器后，可用 const char* 查找 std::string 集合。
    auto it = names.find("alice");
    if (it != names.end()) {
        std::cout << *it << '\n';
    }
}
```

常见透明函数对象还包括算术、比较、逻辑、位运算相关函数对象的 `<>` 形式。

### 2.9 unique_ptr 的优化

C++14 引入 `std::make_unique`，用于安全创建 `unique_ptr`。

```cpp
#include <memory>

class Widget {};

int main() {
    // make_unique 避免直接写 new，异常安全也更好。
    auto p = std::make_unique<Widget>();
    auto arr = std::make_unique<int[]>(10);
}
```

优先使用 `make_unique`，避免裸 `new`，也能让异常安全更容易保证。

补充说明：

C++14 引入 `std::make_unique`，补齐了 C++11 中 `unique_ptr` 创建对象不够方便的问题。它能让代码更简洁，并避免复杂表达式中 `new` 和异常交织带来的资源泄漏风险。

```cpp
#include <iostream>
#include <memory>

struct Task {
    explicit Task(int i) : id(i) {}
    int id;
};

int main() {
    auto p = std::make_unique<Task>(1);    // 创建单个对象
    auto arr = std::make_unique<int[]>(4); // 创建动态数组

    arr[0] = 10;
    std::cout << p->id << ' ' << arr[0] << '\n';
}
```

### 2.10 多线程之读写锁

C++14 引入 `std::shared_timed_mutex`，支持共享读锁和独占写锁，适合读多写少场景。

```cpp
#include <mutex>
#include <shared_mutex>
#include <unordered_map>

class Cache {
public:
    int get(int key) const {
        // 读操作使用共享锁，多个读线程可以同时读取。
        std::shared_lock<std::shared_timed_mutex> lock(mtx_);
        auto it = data_.find(key);
        return it == data_.end() ? -1 : it->second;
    }

    void set(int key, int value) {
        // 写操作使用独占锁，写入时不允许其他读写线程进入。
        std::unique_lock<std::shared_timed_mutex> lock(mtx_);
        data_[key] = value;
    }

private:
    mutable std::shared_timed_mutex mtx_;
    std::unordered_map<int, int> data_;
};
```

读写锁不是总比互斥锁快。只有读操作远多于写操作，且临界区较大时才更可能受益。`std::shared_timed_mutex` 和 `std::shared_lock` 位于 `<shared_mutex>` 中，使用时需要编译器和标准库支持 C++14 共享互斥设施。

---

补充说明：

C++14 提供 `std::shared_timed_mutex` 和 `std::shared_lock`。读写锁允许多个读线程同时持有共享锁，但写线程需要独占锁。读多写少的场景可以提升并发度。

```cpp
#include <iostream>
#include <mutex>
#include <shared_mutex>
#include <string>
#include <utility>
#include <unordered_map>

class Cache {
public:
    std::string get(int key) const {
        std::shared_lock<std::shared_timed_mutex> lock(mtx);
        auto it = data.find(key);
        return it == data.end() ? "" : it->second;
    }

    void set(int key, std::string value) {
        std::unique_lock<std::shared_timed_mutex> lock(mtx);
        data[key] = std::move(value);
    }

private:
    mutable std::shared_timed_mutex mtx;
    std::unordered_map<int, std::string> data;
};
```

## 3 C++17

C++17 继续增强语言表达力和标准库实用性，很多特性直接改善工程代码可读性和安全性。

### 3.1 UTF-8 字面量和十六进制浮点数字面量

C++11 已经支持 `u8"..."` 形式的 UTF-8 字符串字面量；C++17 进一步支持 `u8'...'` 形式的 UTF-8 字符字面量。注意：在 C++17 中，`u8"..."` 的类型仍然是 `const char[]`；到 C++20 后才和 `char8_t` 产生更强关联。

```cpp
auto s = u8"hello"; // auto 推导后变量类型是 const char*；字面量本身类型是 const char[6]
char c = u8'a';     // C++17 支持 UTF-8 字符字面量
```

十六进制浮点数字面量可以精确表达二进制浮点值。

```cpp
double x = 0x1.2p3; // 0x1.2 表示 1 + 2/16，p3 表示乘以 2^3，结果为 9
```

补充说明：

C++11 已支持 `u8"..."` 字符串字面量，C++17 增加 `u8'c'` 字符字面量。C++17 还支持十六进制浮点数字面量，形式类似 `0x1.8p+2`，其中 `p` 后面是以 2 为底的指数。

```cpp
#include <iostream>

int main() {
    char c = u8'a';           // C++17 UTF-8 字符字面量
    double x = 0x1.8p+2;      // 1.5 * 2^2 = 6.0

    std::cout << c << ' ' << x << '\n';
}
```

注意：`char8_t` 是 C++20 才引入的类型，C++17 中 `u8` 字符串元素类型仍是 `char`。

### 3.2 嵌套命名空间

C++17 支持更简洁的嵌套命名空间写法。

```cpp
namespace company::project::module {
    void run() {}
}
```

等价于：

```cpp
namespace company {
namespace project {
namespace module {
    void run() {}
}
}
}
```

补充说明：

C++17 允许把嵌套命名空间写成 `namespace a::b::c {}`，减少重复缩进和样板代码。它等价于逐层写 `namespace a { namespace b { namespace c { ... } } }`。

```cpp
#include <iostream>

namespace net::http::v1 {
void request() {
    std::cout << "request\n";
}
}

int main() {
    net::http::v1::request();
}
```

### 3.3 属性增强

C++17 增加了实用标准属性。

`[[nodiscard]]` 提醒调用者不要忽略返回值。

```cpp
[[nodiscard]] int openFile() {
    return -1;
}

int main() {
    // openFile(); // 编译器可能警告
}
```

`[[maybe_unused]]` 表示变量或函数可能未使用，抑制警告。

```cpp
void f([[maybe_unused]] int debugValue) {}
```

`[[fallthrough]]` 表示 `switch` 中有意贯穿。

```cpp
switch (level) {
case 1:
    // 有意从 case 1 继续执行 case 2。
    prepare();
    [[fallthrough]];
case 2:
    run();
    break;
}
```

补充说明：

C++17 新增常用标准属性：`[[nodiscard]]`、`[[maybe_unused]]`、`[[fallthrough]]`。属性用于给编译器提供额外语义，常用于提高代码质量和减少警告噪声。

```cpp
#include <string>

[[nodiscard]] bool save_to_disk(const std::string& data) {
    return !data.empty();
}

int main() {
    [[maybe_unused]] int debug_only = 42;

    // save_to_disk("x"); // 忽略返回值时，编译器通常会警告。
    bool ok = save_to_disk("x");

    switch (ok ? 1 : 0) {
    case 1:
        // 明确告诉编译器这里有意贯穿到下一个 case。
        [[fallthrough]];
    case 0:
        break;
    }
}
```

### 3.4 类型萃取

C++17 为很多 type traits 提供 `_v` 简写，少写 `::value`。

```cpp
#include <type_traits>

template <typename T>
void f(T value) {
    // _v 是 ::value 的简写，C++17 起标准库大量提供这种变量模板。
    static_assert(std::is_integral_v<T>, "T must be integral");
}
```

还提供 `_t` 类型别名形式，例如：

```cpp
#include <type_traits>

using T = std::remove_reference_t<int&>; // int
```

补充说明：

类型萃取位于 `<type_traits>`，用于在编译期查询、修改和组合类型信息。C++17 补充了 `_v` 变量模板简写、`std::conjunction`、`std::disjunction`、`std::negation`、`std::is_invocable` 等。

```cpp
#include <iostream>
#include <type_traits>

template <typename T>
void print_number(T value) {
    if constexpr (std::is_arithmetic_v<T>) {
        std::cout << value << '\n';
    } else {
        static_assert(std::is_arithmetic_v<T>, "T must be arithmetic");
    }
}

int main() {
    static_assert(std::is_same_v<int, std::remove_const_t<const int>>);
    static_assert(std::conjunction_v<std::is_integral<int>, std::is_signed<int>>);
    print_number(10);
}
```

### 3.5 编译期判断语句 constexpr if

`if constexpr` 是编译期分支，不满足的分支不会被实例化，非常适合模板代码。

```cpp
#include <iostream>
#include <type_traits>

template <typename T>
void print(const T& value) {
    if constexpr (std::is_integral_v<T>) {
        std::cout << "integer: " << value << '\n';
    } else {
        std::cout << "other: " << value << '\n';
    }
}
```

普通 `if` 的两个分支都要能编译，`if constexpr` 则可以按类型选择有效分支。

补充说明：

`if constexpr` 在编译期选择分支，未被选中的分支不会参与实例化。它特别适合模板代码中根据类型选择不同逻辑，避免大量 `enable_if`。

```cpp
#include <iostream>
#include <string>
#include <type_traits>

template <typename T>
void print(const T& value) {
    if constexpr (std::is_pointer_v<T>) {
        std::cout << *value << '\n'; // 只有指针类型会实例化此分支。
    } else {
        std::cout << value << '\n';
    }
}

int main() {
    int x = 10;
    print(x);
    print(&x);
}
```

`if constexpr` 的条件必须是编译期常量表达式，它不能替代普通运行时 `if`。

### 3.6 if/switch 语句中的初始化器

C++17 允许在 `if` 和 `switch` 中写初始化语句，限制变量作用域。

```cpp
#include <map>
#include <string>

int main() {
    std::map<std::string, int> scores{{"Alice", 90}};

    // 初始化语句中的 it 只在 if/else 内部可见。
    if (auto it = scores.find("Alice"); it != scores.end()) {
        it->second += 5;
    }
}
```

`it` 只在 `if/else` 范围内有效，能减少变量污染。

补充说明：

C++17 允许在 `if` 和 `switch` 的条件前写初始化语句，变量作用域只在该语句内部，能减少临时变量外泄。

```cpp
#include <iostream>
#include <map>
#include <string>

int main() {
    std::map<int, std::string> users{{1, "Tom"}};

    if (auto it = users.find(1); it != users.end()) {
        std::cout << it->second << '\n';
    } else {
        std::cout << "not found\n";
    }

    switch (int code = 2; code) {
    case 1:
        break;
    case 2:
        std::cout << "two\n";
        break;
    }
}
```

### 3.7 constexpr Lambda 和捕获 *this 的拷贝

C++17 中，满足条件的 lambda 可以在编译期调用。

```cpp
constexpr auto add = [](int a, int b) {
    return a + b;
};

static_assert(add(1, 2) == 3);
```

C++17 支持 `[*this]`，表示拷贝当前对象，而不是只捕获 `this` 指针。

```cpp
class Counter {
public:
    auto makeFunc() const {
        // [*this] 拷贝当前对象，避免只捕获 this 指针导致悬空。
        return [*this] {
            return value_;
        };
    }

private:
    int value_ = 10;
};
```

异步场景中拷贝 `*this` 能降低悬空指针风险，但也要注意拷贝成本。

补充说明：

C++17 中 lambda 在满足条件时可以是 `constexpr`，可以用于编译期计算。C++17 还支持 `[*this]` 捕获当前对象的副本，避免异步执行时对象已经销毁导致悬空引用。

```cpp
#include <iostream>

struct Counter {
    int base = 10;

    auto make_task() const {
        return [*this](int x) {
            // 捕获的是当前对象副本，不依赖原对象生命周期。
            return base + x;
        };
    }
};

int main() {
    constexpr auto square = [](int x) constexpr {
        return x * x;
    };
    static_assert(square(3) == 9);

    Counter c;
    auto task = c.make_task();
    std::cout << task(5) << '\n';
}
```

### 3.8 枚举的直接列表初始化

C++17 允许使用直接列表初始化枚举对象，前提是不发生窄化转换。

```cpp
enum class Byte : unsigned char {};

Byte b{42};
```

这常用于创建强类型整数包装，增强类型安全。

补充说明：

C++17 允许对带固定底层类型的枚举进行直接列表初始化。它要求不能发生窄化转换。该特性让强类型枚举更适合表示底层字节、协议字段等。

```cpp
#include <cstdint>
#include <iostream>

enum class Code : std::uint8_t {};

int main() {
    Code c{42}; // 直接列表初始化，底层类型为 uint8_t。
    std::cout << static_cast<int>(c) << '\n';
}
```

### 3.9 内联变量

C++17 引入 `inline` 变量，允许在头文件中定义全局变量或类静态成员，而不会违反 ODR。

```cpp
// config.h
inline int defaultPort = 8080;

struct Config {
    // inline static 成员可以直接在类内定义，头文件多次包含也不会重复定义。
    inline static const char* name = "server";
};
```

这简化了头文件常量和类静态成员定义。

补充说明：

C++17 的 `inline` 变量解决头文件中定义全局变量或类静态成员的 ODR 问题。内联变量可以在多个翻译单元中定义，链接时视为同一个实体。

```cpp
// config.h
#pragma once
#include <string>

inline int max_connections = 1024;
inline const std::string app_name = "server";

struct Settings {
    inline static int timeout = 30; // C++17 可在类内定义 inline static 数据成员。
};
```

适合头文件中的全局常量、配置项、类静态成员。不应滥用可变全局状态。

### 3.10 结构化绑定

结构化绑定可以把数组、结构体、`pair`、`tuple` 中的元素拆成多个变量。

```cpp
#include <map>
#include <string>

int main() {
    std::map<std::string, int> scores{{"Alice", 90}};

    for (const auto& [name, score] : scores) {
        // name 和 score 分别绑定 key/value
    }
}
```

函数返回多个值时也很有用：

```cpp
#include <tuple>

std::tuple<int, int> getRange() {
    return {1, 10};
}

auto [begin, end] = getRange();
```

补充说明：

结构化绑定可以把数组、结构体、`std::pair`、`std::tuple` 等对象拆成多个名字。常用于遍历 `map`、处理函数多返回值。

```cpp
#include <iostream>
#include <map>
#include <string>
#include <tuple>

std::tuple<int, std::string> query() {
    return {200, "ok"};
}

int main() {
    auto [code, message] = query();
    std::cout << code << ' ' << message << '\n';

    std::map<int, std::string> users{{1, "Tom"}};
    for (const auto& [id, name] : users) {
        std::cout << id << ": " << name << '\n';
    }
}
```

结构化绑定默认按值绑定，若要避免拷贝或修改原对象，可使用 `auto&` 或 `const auto&`。

### 3.11 带有基类的聚合类

C++17 放宽聚合类规则，允许带有公开基类的类作为聚合类型。

```cpp
struct Base {
    int x;
};

struct Derived : Base {
    int y;
};

Derived d{{1}, 2};
```

这让简单数据结构继承时仍能使用聚合初始化。

补充说明：

C++17 放宽聚合类规则，允许聚合类拥有公开基类。这样带基类的简单数据结构也能使用聚合初始化。

```cpp
#include <iostream>

struct Point {
    int x;
    int y;
};

struct ColorPoint : Point {
    int color;
};

int main() {
    ColorPoint p{{1, 2}, 255}; // 先初始化基类 Point，再初始化派生类成员。
    std::cout << p.x << ' ' << p.y << ' ' << p.color << '\n';
}
```

若存在用户自定义构造函数、私有或受保护非静态数据成员等，类型通常不再是聚合类。

### 3.12 折叠表达式

折叠表达式简化可变参数模板中的递归展开。

```cpp
#include <iostream>

template <typename... Args>
auto sum(Args... args) {
    return (args + ...);
}

template <typename... Args>
void printAll(const Args&... args) {
    ((std::cout << args << ' '), ...);
}

int main() {
    std::cout << sum(1, 2, 3) << '\n';
    printAll("C++", 17, "fold");
}
```

常见形式：

- `(args + ...)`
- `(... + args)`
- `(init + ... + args)`
- `(args + ... + init)`

补充说明：

折叠表达式用于展开参数包，简化可变参数模板。四种形式分别是一元左折叠、一元右折叠、二元左折叠、二元右折叠。空参数包只有少数运算符有默认结果：`&&` 为 `true`，`||` 为 `false`，`,` 为 `void()`。

```cpp
#include <iostream>

template <typename... Args>
auto sum(Args... args) {
    return (args + ...); // 一元右折叠：a + (b + c)
}

template <typename... Args>
void print_all(const Args&... args) {
    ((std::cout << args << ' '), ...); // 用逗号折叠依次输出。
    std::cout << '\n';
}

int main() {
    std::cout << sum(1, 2, 3) << '\n';
    print_all("a", 1, 2.5);
}
```

### 3.13 带有 auto 类型的非类型模板参数

C++17 允许非类型模板参数使用 `auto` 自动推导类型。

```cpp
template <auto N>
struct Constant {
    static constexpr auto value = N;
};

int main() {
    Constant<10> a;
    Constant<'x'> b;
}
```

这让模板常量参数更灵活。

补充说明：

C++17 允许非类型模板参数使用 `auto`，由实参自动推导类型。它适合封装编译期常量、策略值、状态机状态等。

```cpp
#include <iostream>

template <auto N>
struct Constant {
    static constexpr auto value = N;
};

template <auto Func>
void call() {
    Func(); // Func 可以是函数指针这类非类型模板实参。
}

void hello() {
    std::cout << "hello\n";
}

int main() {
    static_assert(Constant<42>::value == 42);
    static_assert(Constant<'A'>::value == 'A');
    call<hello>();
}
```

### 3.14 类模板参数推导

C++17 支持根据构造函数参数推导类模板参数，称为 CTAD。

```cpp
#include <cstddef>
#include <utility>
#include <vector>

int main() {
    // C++17 可根据初始化参数推导类模板参数。
    std::pair p{1, 3.14};          // std::pair<int, double>
    std::vector v{1, 2, 3, 4};     // std::vector<int>
}
```

自定义类也可以提供推导指引。

```cpp
#include <string>

template <typename T>
class Box {
public:
    Box(T value) : value_(value) {}
private:
    T value_;
};

// 推导指引：const char* 构造 Box 时，将 T 推导为 std::string。
Box(const char*) -> Box<std::string>;
```

补充说明：

C++17 支持类模板参数推导，构造类模板对象时可以省略模板实参。标准库中 `std::pair`、`std::tuple`、`std::vector` 等都可受益。自定义类模板可通过推导指引帮助编译器推导类型。

```cpp
#include <iostream>
#include <string>
#include <utility>

template <typename T>
struct Box {
    T value;
};

// C++17 中聚合类通常需要显式推导指引。
template <typename T>
Box(T) -> Box<T>;

int main() {
    std::pair p(1, std::string("Tom")); // 推导为 pair<int, string>
    Box b{42};                          // 通过推导指引推导为 Box<int>

    std::cout << p.second << ' ' << b.value << '\n';
}
```

推导指引分为隐式推导指引和用户自定义推导指引。构造函数模板、万能引用、类型转换和别名场景都可能影响推导结果。

### 3.15 原始字节类型 std::byte

`std::byte` 表示原始字节，定义在 `<cstddef>` 中。它不是整数类型，避免把字节数据误当数值运算。

```cpp
#include <cstddef>
#include <iostream>

int main() {
    // std::byte 表示原始字节，不直接参与整数四则运算。
    std::byte b{0x2a};
    auto value = std::to_integer<int>(b);
    std::cout << value << '\n';
}
```

适合二进制协议、文件内容、内存缓冲区等场景。

补充说明：

`std::byte` 表示原始字节，位于 `<cstddef>`。它不是字符类型，也不是整数类型，不能直接做算术运算，但支持位运算。需要转为整数时使用 `std::to_integer<T>`。

```cpp
#include <cstddef>
#include <iostream>

int main() {
    std::byte b{0x0f};
    b = b | std::byte{0xf0}; // 支持按位或。

    int value = std::to_integer<int>(b);
    std::cout << value << '\n';
}
```

`std::byte` 适合网络包、二进制文件、内存缓冲区等场景，能避免把原始字节误当字符或数字参与计算。

### 3.16 std::as_const

`std::as_const` 把左值转换成 const 左值引用，常用于强制调用 const 重载。

```cpp
#include <cstddef>
#include <utility>
#include <vector>

class Data {
public:
    int& at(std::size_t i) { return values_[i]; }
    const int& at(std::size_t i) const { return values_[i]; }

private:
    std::vector<int> values_{1, 2, 3};
};

int main() {
    Data d;
    auto x = std::as_const(d).at(0); // 调用 const 版本
}
```

`as_const` 不接受右值，避免返回悬空引用。

补充说明：

`std::as_const` 位于 `<utility>`，用于把左值引用转换为 `const` 左值引用。它可强制调用对象的 `const` 成员函数、防止循环中意外修改对象、复用 `const` 逻辑。它不能用于右值。

```cpp
#include <iostream>
#include <utility>
#include <vector>

int main() {
    std::vector<int> v{1, 2, 3};

    for (const auto& x : std::as_const(v)) {
        // x 只能读取，不能修改。
        std::cout << x << '\n';
    }
}
```

### 3.17 std::invoke

`std::invoke` 是通用调用工具，可以统一调用普通函数、函数对象、成员函数指针、成员变量指针。

```cpp
#include <functional>
#include <iostream>

struct Foo {
    int value = 10;
    int add(int x) const {
        return value + x;
    }
};

int main() {
    Foo foo;
    std::cout << std::invoke(&Foo::add, foo, 5) << '\n';
    std::cout << std::invoke(&Foo::value, foo) << '\n';
}
```

泛型库中常用 `invoke` 实现统一的可调用对象处理。

补充说明：

`std::invoke` 位于 `<functional>`，可以统一调用普通函数、函数对象、lambda、成员函数指针、数据成员指针。它是很多泛型调用工具的底层基础。

```cpp
#include <functional>
#include <iostream>

struct User {
    int id = 10;

    void print(const char* prefix) const {
        std::cout << prefix << id << '\n';
    }
};

int add(int a, int b) {
    return a + b;
}

int main() {
    User u;
    std::cout << std::invoke(add, 1, 2) << '\n';
    std::invoke(&User::print, u, "id=");
    std::cout << std::invoke(&User::id, u) << '\n';
}
```

### 3.18 std::apply / std::make_from_tuple

`std::apply` 可以把 tuple 解包后作为函数参数调用。

```cpp
#include <iostream>
#include <tuple>

int add(int a, int b) {
    return a + b;
}

int main() {
    auto args = std::make_tuple(1, 2);
    std::cout << std::apply(add, args) << '\n';
}
```

`std::make_from_tuple` 用 tuple 中的元素构造对象。

```cpp
#include <string>
#include <tuple>

struct Person {
    Person(std::string name, int age) {}
};

int main() {
    auto args = std::make_tuple(std::string("Alice"), 18);
    auto p = std::make_from_tuple<Person>(args);
}
```

补充说明：

`std::apply` 把 tuple 中的元素解包为函数参数。`std::make_from_tuple<T>` 用 tuple 中的元素构造对象。它们适合参数打包、延迟调用、工厂函数等场景。

```cpp
#include <iostream>
#include <string>
#include <tuple>
#include <utility>

struct User {
    User(int i, std::string n) : id(i), name(std::move(n)) {}
    int id;
    std::string name;
};

int add(int a, int b, int c) {
    return a + b + c;
}

int main() {
    auto args = std::make_tuple(1, 2, 3);
    std::cout << std::apply(add, args) << '\n';

    auto user_args = std::make_tuple(7, std::string("Tom"));
    User u = std::make_from_tuple<User>(user_args);
    std::cout << u.name << '\n';
}
```

### 3.19 std::not_fn

`std::not_fn` 对可调用对象结果取反，替代旧的 `not1/not2`。

```cpp
#include <algorithm>
#include <functional>
#include <vector>

bool isEven(int x) {
    return x % 2 == 0;
}

int main() {
    std::vector<int> nums{1, 2, 3, 4};
    auto countOdd = std::count_if(nums.begin(), nums.end(), std::not_fn(isEven));
}
```

补充说明：

`std::not_fn` 位于 `<functional>`，用于对可调用对象的结果取逻辑非。相比旧的 `std::not1`、`std::not2`，它更通用，能处理 lambda、多参数函数、成员函数等。

```cpp
#include <algorithm>
#include <functional>
#include <iostream>
#include <vector>

int main() {
    std::vector<int> v{1, 2, 3, 4, 5};

    auto is_even = [](int x) {
        return x % 2 == 0;
    };

    auto it = std::find_if(v.begin(), v.end(), std::not_fn(is_even));
    if (it != v.end()) {
        std::cout << "first odd: " << *it << '\n';
    }
}
```

### 3.20 关联容器的改进

C++17 为 `map/set/unordered_map/unordered_set` 增加了实用能力，例如节点句柄、`try_emplace`、`insert_or_assign`。

`try_emplace` 只在 key 不存在时构造 value。

```cpp
#include <map>
#include <string>

int main() {
    std::map<int, std::string> m;

    m.try_emplace(1, "Alice");
    m.try_emplace(1, "Bob"); // key 已存在，不修改
}
```

`insert_or_assign` 在 key 不存在时插入，存在时赋值。

```cpp
m.insert_or_assign(1, "Cindy");
```

节点句柄可以在容器之间移动节点，避免重新分配元素。

```cpp
#include <map>
#include <string>
#include <utility>

std::map<int, std::string> a{{1, "one"}};
std::map<int, std::string> b;

auto node = a.extract(1);
b.insert(std::move(node));
```

补充说明：

C++17 为关联容器和无序关联容器增加节点句柄、`extract`、`merge`、`try_emplace`、`insert_or_assign` 等能力。节点句柄可以在不重新构造元素的情况下转移节点；`try_emplace` 避免 key 已存在时仍构造 mapped value。

```cpp
#include <iostream>
#include <map>
#include <string>
#include <utility>

int main() {
    std::map<int, std::string> a{{1, "one"}};
    std::map<int, std::string> b{{2, "two"}};

    auto node = a.extract(1); // 提取节点，a 中不再有 key 1。
    node.key() = 3;           // map 节点句柄允许修改 key。
    b.insert(std::move(node));

    b.try_emplace(2, "ignored");      // key 已存在，不构造新 value。
    b.insert_or_assign(2, "updated"); // key 已存在则赋值。

    for (const auto& [k, v] : b) {
        std::cout << k << ": " << v << '\n';
    }
}
```

### 3.21 std::variant

`std::variant` 是类型安全的联合体，一个对象同一时刻保存多个候选类型之一。

```cpp
#include <iostream>
#include <string>
#include <variant>

using Value = std::variant<int, double, std::string>;

int main() {
    Value v = std::string("hello");

    // visit 会根据 variant 当前保存的真实类型调用 lambda。
    std::visit([](const auto& x) {
        std::cout << x << '\n';
    }, v);
}
```

适合配置值、消息体、语法树节点等场景。

补充说明：

`std::variant` 是类型安全的联合体，同一时间只保存给定类型列表中的一种类型。可用 `std::get`、`std::get_if` 访问，用 `std::visit` 统一处理不同类型。

```cpp
#include <iostream>
#include <string>
#include <variant>

int main() {
    std::variant<int, std::string> value = 42;
    value = std::string("hello");

    if (auto p = std::get_if<std::string>(&value)) {
        std::cout << *p << '\n';
    }

    std::visit([](const auto& x) {
        std::cout << x << '\n';
    }, value);
}
```

若用错误类型调用 `std::get`，会抛 `std::bad_variant_access`。当赋值或构造异常导致没有有效值时，`variant` 可能处于 `valueless_by_exception` 状态。

### 3.22 std::any

`std::any` 可以保存任意可拷贝类型，取值时需要知道真实类型。

```cpp
#include <any>
#include <iostream>
#include <string>

int main() {
    std::any value = std::string("hello");

    // any_cast 前最好确认真实类型，否则类型不匹配会抛 bad_any_cast。
    if (value.type() == typeid(std::string)) {
        std::cout << std::any_cast<std::string>(value) << '\n';
    }
}
```

`any` 灵活但类型约束弱。能用明确类型就不用 `any`，能用 `variant` 就优先用 `variant`。

补充说明：

`std::any` 可以保存任意可拷贝构造类型，适合配置项、异构容器等场景。读取时必须指定正确类型，错误会抛 `std::bad_any_cast`，或者使用指针形式的 `std::any_cast` 做安全检查。

```cpp
#include <any>
#include <iostream>
#include <string>
#include <unordered_map>

int main() {
    std::unordered_map<std::string, std::any> config;
    config["port"] = 8080;
    config["host"] = std::string("127.0.0.1");

    if (auto p = std::any_cast<int>(&config["port"])) {
        std::cout << *p << '\n';
    }
}
```

`std::any` 会隐藏具体类型，不适合滥用为“万能参数”。类型集合固定时优先考虑 `std::variant`。

### 3.23 std::optional

`std::optional<T>` 表示可能有值，也可能没有值。

```cpp
#include <optional>
#include <string>

std::optional<int> parseInt(const std::string& s) {
    try {
        return std::stoi(s);
    } catch (...) {
        return std::nullopt;
    }
}

int main() {
    if (auto value = parseInt("123")) {
        // optional 有值时才能解引用。
        int x = *value;
    }
}
```

它比返回特殊值更清晰，也比滥用异常更适合“无结果是正常情况”的场景。

补充说明：

`std::optional<T>` 表示“可能有 T，也可能没有值”。它比用特殊值、裸指针或额外 bool 更清晰。常用操作有 `has_value()`、显式转 bool、`value()`、`value_or()`、`reset()`、`emplace()`。

```cpp
#include <cstddef>
#include <iostream>
#include <optional>
#include <string>

std::optional<int> parse_port(const std::string& s) {
    if (s.empty()) {
        return std::nullopt; // 明确表示没有值。
    }

    try {
        std::size_t pos = 0;
        int port = std::stoi(s, &pos);
        if (pos != s.size()) {
            return std::nullopt; // 还有未解析字符，认为解析失败。
        }
        return port;
    } catch (...) {
        return std::nullopt; // 格式非法或数值越界时返回空 optional。
    }
}

int main() {
    auto port = parse_port("8080");
    if (port) {
        std::cout << *port << '\n';
    }

    std::cout << parse_port("").value_or(80) << '\n';
}
```

对空 `optional` 调用 `value()` 会抛 `std::bad_optional_access`。

### 3.24 std::string_view

`std::string_view` 是非拥有字符串视图，只保存指针和长度，不负责管理字符串生命周期。

```cpp
#include <cstddef>
#include <iostream>
#include <string>
#include <string_view>

void print(std::string_view s) {
    // string_view 只读不拥有，不会拷贝底层字符串。
    std::cout << s << '\n';
}

int main() {
    print("hello");
    std::string name = "Alice";
    print(name);
}
```

不要返回指向局部字符串的 `string_view`：

```cpp
#include <string>
#include <string_view>

std::string_view bad() {
    std::string s = "danger";
    return s; // 错误：悬空
}
```

补充说明：

`std::string_view` 是非拥有字符串视图，只保存指针和长度，不管理内存。它适合作为只读参数、切片和解析场景，可避免复制。最大风险是悬空引用：不要让 `string_view` 指向已经销毁的临时字符串。

```cpp
#include <iostream>
#include <string>
#include <string_view>

void print_prefix(std::string_view s) {
    s.remove_prefix(1); // 只移动视图起点，不修改原字符串。
    std::cout << s << '\n';
}

int main() {
    std::string text = "hello";
    print_prefix(text);

    // std::string_view bad = std::string("tmp"); // 危险：临时 string 销毁后悬空。
}
```

`string_view` 不保证以 `\0` 结尾，传给 C 风格 API 时要格外小心。

### 3.25 字符转换

C++17 引入 `<charconv>`，提供 `std::to_chars` 和 `std::from_chars`。它们不依赖 locale，不抛异常，适合高性能数值和字符串转换。

```cpp
#include <charconv>
#include <iostream>
#include <system_error>

int main() {
    char buf[32];
    int value = 12345;

    // to_chars 不分配内存、不抛异常，通过错误码报告结果。
    auto [ptr, ec] = std::to_chars(buf, buf + sizeof(buf), value);
    if (ec == std::errc{}) {
        std::cout.write(buf, ptr - buf);
    }
}
```

字符串转整数：

```cpp
#include <charconv>
#include <stdexcept>
#include <string_view>
#include <system_error>

int parse(std::string_view s) {
    int value = 0;
    auto [ptr, ec] = std::from_chars(s.data(), s.data() + s.size(), value);
    if (ec != std::errc{} || ptr != s.data() + s.size()) {
        throw std::invalid_argument("invalid integer");
    }
    return value;
}
```

补充说明：

`<charconv>` 提供 `std::to_chars` 和 `std::from_chars`，用于数值和字符序列之间的高性能转换。它们不分配内存、不依赖 locale、不抛异常，通过返回的错误码表示结果。

```cpp
#include <charconv>
#include <iostream>
#include <string>
#include <string_view>
#include <system_error>

int main() {
    char buf[32]{};
    auto [ptr, ec1] = std::to_chars(buf, buf + 32, 12345);
    if (ec1 == std::errc{}) {
        std::cout << std::string_view(buf, ptr - buf) << '\n';
    }

    int value = 0;
    std::string_view s = "6789";
    auto [end, ec2] = std::from_chars(s.data(), s.data() + s.size(), value);
    if (ec2 == std::errc{} && end == s.data() + s.size()) {
        std::cout << value << '\n';
    }
}
```

`from_chars` 不会自动跳过前导空白，也不会像 `stoi` 那样抛异常。

### 3.26 智能指针的增强

C++17 增强了 `std::shared_ptr` 对数组类型的支持，可以直接使用 `std::shared_ptr<T[]>` 并通过下标访问元素。C++11 中也可以让 `shared_ptr` 管理数组，但通常要手动指定删除器，写法更容易出错。

```cpp
#include <memory>

int main() {
    // C++17 shared_ptr<T[]> 会使用 delete[] 释放数组。
    std::shared_ptr<int[]> arr(new int[10]);
    arr[0] = 1;
}
```

C++17 还支持 `std::weak_from_this`，它来自 `enable_shared_from_this`，可安全获得 `weak_ptr`。

```cpp
#include <memory>

class Session : public std::enable_shared_from_this<Session> {
public:
    std::weak_ptr<Session> getWeak() {
        // weak_from_this 不增加引用计数，适合把弱引用交给异步任务或观察者。
        return weak_from_this();
    }
};
```

补充说明：

C++17 增强了 `std::shared_ptr<T[]>` 对数组类型的支持，可以使用下标访问，删除时使用 `delete[]`。`std::weak_ptr<T[]>` 可从 `shared_ptr<T[]>` 观察数组所有权，`lock()` 后得到 `shared_ptr<T[]>`。

```cpp
#include <iostream>
#include <memory>

int main() {
    std::shared_ptr<int[]> data(new int[3]{1, 2, 3});
    std::weak_ptr<int[]> weak = data;

    if (auto locked = weak.lock()) {
        // C++17 shared_ptr<T[]> 支持 operator[]。
        std::cout << locked[1] << '\n';
    }
}
```

C++11 中也可以让 `shared_ptr` 管理数组，但通常要手动指定删除器，写法更容易出错。

### 3.27 std::owner_less

`std::owner_less` 按智能指针的所有权关系比较，而不是按裸指针地址比较。它适合把 `shared_ptr` 或 `weak_ptr` 放入有序容器。

```cpp
#include <map>
#include <memory>

int main() {
    std::map<std::weak_ptr<int>, int, std::owner_less<std::weak_ptr<int>>> m;

    auto p = std::make_shared<int>(10);
    // weak_ptr 可以作为 key，但比较规则应使用 owner_less。
    m[p] = 1;
}
```

补充说明：

`std::owner_less` 按智能指针控制块的所有权关系比较，而不是按裸指针地址比较。C++11 已有针对 `shared_ptr` 和 `weak_ptr` 的 `owner_less`；C++17 增加 `std::owner_less<void>`，可透明比较不同智能指针类型。

```cpp
#include <iostream>
#include <map>
#include <memory>

int main() {
    auto sp = std::make_shared<int>(10);
    std::weak_ptr<int> wp = sp;

    // C++17 owner_less<void> 可以混合比较 shared_ptr 和 weak_ptr 的所有权。
    std::owner_less<void> cmp;
    std::cout << cmp(sp, wp) << '\n';

    std::map<std::weak_ptr<int>, int, std::owner_less<std::weak_ptr<int>>> table;
    table[wp] = 1;
}
```

这适合把 `weak_ptr` 作为有序容器 key，避免对象地址变化或别名指针带来的比较问题。

### 3.28 多线程之读写锁

C++17 提供 `std::shared_mutex`，比 C++14 的 `shared_timed_mutex` 更直接。读线程使用共享锁，写线程使用独占锁。

```cpp
#include <mutex>
#include <shared_mutex>
#include <unordered_map>

class Cache {
public:
    int get(int key) const {
        // 读操作使用共享锁，多个读线程可并发进入。
        std::shared_lock<std::shared_mutex> lock(mtx_);
        auto it = data_.find(key);
        return it == data_.end() ? -1 : it->second;
    }

    void set(int key, int value) {
        // 写操作使用独占锁，避免和读写操作并发修改数据。
        std::unique_lock<std::shared_mutex> lock(mtx_);
        data_[key] = value;
    }

private:
    mutable std::shared_mutex mtx_;
    std::unordered_map<int, int> data_;
};
```

补充说明：

C++17 提供 `std::shared_mutex`。它和 C++14 的 `std::shared_timed_mutex` 类似，但不提供超时接口。读线程使用 `std::shared_lock` 获取共享锁，写线程使用 `std::unique_lock` 获取独占锁。

```cpp
#include <iostream>
#include <mutex>
#include <shared_mutex>
#include <unordered_map>

class Dict {
public:
    int get(int key) const {
        std::shared_lock<std::shared_mutex> lock(mtx);
        auto it = data.find(key);
        return it == data.end() ? 0 : it->second;
    }

    void set(int key, int value) {
        std::unique_lock<std::shared_mutex> lock(mtx);
        data[key] = value;
    }

private:
    mutable std::shared_mutex mtx;
    std::unordered_map<int, int> data;
};
```

读写锁适合读多写少场景。若写操作频繁或临界区很短，普通互斥锁可能更简单、更快。

### 3.29 搜索器

C++17 提供搜索器，可与 `std::search` 配合使用，例如 Boyer-Moore 搜索器。

```cpp
#include <algorithm>
#include <functional>
#include <iostream>
#include <string>

int main() {
    std::string text = "hello modern c++17";
    std::string pattern = "modern";

    // 搜索器会预处理 pattern，适合对同一模式进行多次搜索。
    auto searcher = std::boyer_moore_searcher(pattern.begin(), pattern.end());
    auto it = std::search(text.begin(), text.end(), searcher);

    if (it != text.end()) {
        std::cout << "found\n";
    }
}
```

适合在较长文本中多次查找同一模式串。

补充说明：

C++17 在 `<functional>` 中提供搜索器，可配合 `std::search` 使用，包括 `std::default_searcher`、`std::boyer_moore_searcher`、`std::boyer_moore_horspool_searcher`。对大文本或重复搜索同一模式串时，后两者可能更高效。

```cpp
#include <algorithm>
#include <functional>
#include <iostream>
#include <string>

int main() {
    std::string text = "hello modern c++17";
    std::string pattern = "modern";

    auto searcher = std::boyer_moore_searcher(pattern.begin(), pattern.end());
    auto it = std::search(text.begin(), text.end(), searcher);

    if (it != text.end()) {
        std::cout << "found at " << (it - text.begin()) << '\n';
    }
}
```

自定义数据类型也能搜索，只要提供合适的相等比较和哈希等条件。

### 3.30 并行算法

C++17 为部分标准算法加入执行策略，允许并行或向量化执行。

```cpp
#include <algorithm>
#include <execution>
#include <vector>

int main() {
    std::vector<int> nums(1000000, 1);

    // par 表示允许标准库并行执行排序，具体效果依赖实现和环境。
    std::sort(std::execution::par, nums.begin(), nums.end());
}
```

常见执行策略：

- `std::execution::seq`：顺序执行；
- `std::execution::par`：并行执行；
- `std::execution::par_unseq`：并行且允许向量化。

使用并行算法时，传入的操作不能有数据竞争，也不要依赖执行顺序。

补充说明：

C++17 为部分标准算法增加执行策略，位于 `<execution>`：`std::execution::seq` 顺序执行，`par` 并行执行，`par_unseq` 允许并行且向量化。并行算法要求回调中不要产生数据竞争，也不要依赖执行顺序。

```cpp
#include <algorithm>
#include <execution>
#include <iostream>
#include <numeric>
#include <vector>

int main() {
    std::vector<int> v(1000);
    std::iota(v.begin(), v.end(), 1);

    std::sort(std::execution::par, v.begin(), v.end());

    // reduce 可并行归约，操作应满足结合律，避免依赖顺序。
    int total = std::reduce(std::execution::par, v.begin(), v.end(), 0);
    std::cout << total << '\n';
}
```

常见新增或增强算法包括 `std::reduce`、`std::transform_reduce`、`std::exclusive_scan`、`std::inclusive_scan`、`std::transform_exclusive_scan`、`std::transform_inclusive_scan`。在 `par_unseq` 下不能在回调中加锁或访问有数据竞争的共享状态。

### 3.31 文件系统：路径、状态和属性

C++17 标准化 `<filesystem>`，提供跨平台路径和文件属性操作。

```cpp
#include <filesystem>
#include <iostream>

namespace fs = std::filesystem;

int main() {
    fs::path p = "test.txt";

    // path 提供跨平台路径拆分能力。
    std::cout << p.filename().string() << '\n';
    std::cout << p.extension().string() << '\n';

    // 查询文件属性前先判断路径是否存在。
    if (fs::exists(p)) {
        std::cout << fs::file_size(p) << '\n';
    }
}
```

常用能力：

- `fs::path`：路径对象；
- `exists`：判断是否存在；
- `is_regular_file`：是否普通文件；
- `is_directory`：是否目录；
- `file_size`：文件大小；
- `last_write_time`：最后修改时间。

补充说明：

`std::filesystem` 提供跨平台文件系统操作。核心类型是 `std::filesystem::path`，核心能力包括路径拼接、路径分解、存在性检查、文件类型判断、文件大小、最后修改时间、权限等。可能抛出 `std::filesystem::filesystem_error`，也有带 `std::error_code` 的无异常重载。

```cpp
#include <filesystem>
#include <iostream>
#include <system_error>

namespace fs = std::filesystem;

int main() {
    fs::path p = fs::path("logs") / "app.log"; // 使用 / 拼接路径。

    std::cout << p.parent_path() << '\n';
    std::cout << p.filename() << '\n';
    std::cout << p.extension() << '\n';

    std::error_code ec;
    fs::file_status st = fs::status(p, ec);
    if (!ec && fs::is_regular_file(st)) {
        std::cout << fs::file_size(p, ec) << '\n';
    }
}
```

路径字符串的编码和分隔符在不同平台上可能不同，优先使用 `path` 的接口处理路径，而不是手工拼接字符串。

### 3.32 文件系统：文件与目录 CRUD

`std::filesystem` 还支持目录遍历、创建、删除、复制、重命名等操作。

```cpp
#include <filesystem>
#include <iostream>

namespace fs = std::filesystem;

int main() {
    // 递归创建目录，父目录不存在时也会一并创建。
    fs::create_directories("logs/2026");

    for (const auto& entry : fs::directory_iterator(".")) {
        // directory_iterator 只遍历当前目录一层。
        std::cout << entry.path().string() << '\n';
    }

    // overwrite_existing 表示目标存在时覆盖。
    fs::copy_file("a.txt", "b.txt", fs::copy_options::overwrite_existing);
    fs::rename("b.txt", "c.txt");
    fs::remove("c.txt");
}
```

递归遍历目录：

```cpp
for (const auto& entry : fs::recursive_directory_iterator(".")) {
    if (entry.is_regular_file()) {
        std::cout << entry.path() << '\n';
    }
}
```

文件系统操作可能抛出 `std::filesystem::filesystem_error`，工程代码中要做好异常处理或使用带 `std::error_code` 的重载。

补充说明：

`std::filesystem` 支持创建、复制、移动、删除、遍历文件和目录。常用函数包括 `create_directory`、`create_directories`、`copy`、`copy_file`、`rename`、`remove`、`remove_all`、`directory_iterator`、`recursive_directory_iterator`。

```cpp
#include <filesystem>
#include <iostream>
#include <system_error>

namespace fs = std::filesystem;

int main() {
    std::error_code ec;

    fs::create_directories("logs/2026", ec); // 递归创建目录。
    if (ec) {
        std::cout << "create failed: " << ec.message() << '\n';
    }

    for (const auto& entry : fs::directory_iterator(".")) {
        // directory_iterator 只遍历当前目录一层。
        std::cout << entry.path() << '\n';
    }

    if (fs::exists("a.txt")) {
        fs::copy_file("a.txt", "b.txt", fs::copy_options::overwrite_existing, ec);
        fs::rename("b.txt", "c.txt", ec);
        fs::remove("c.txt", ec);
    }
}
```

递归删除要非常谨慎，`remove_all` 会删除目录下所有内容。工程代码中建议先打印或校验目标路径，再执行删除操作。
